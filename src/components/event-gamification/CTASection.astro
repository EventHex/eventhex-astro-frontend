---
---

<section class="egcta">
  <div class="egcta__card">
    <div class="egcta__glow egcta__glow--1"></div>
    <div class="egcta__glow egcta__glow--2"></div>
    <div class="egcta__content">
      <span class="egcta__eyebrow animate-on-scroll" data-animation="fadeIn">Gamify Your Next Event</span>
      <h2 class="egcta__title animate-on-scroll" data-animation="fadeInUp">Ready to 3x Your Event Engagement?</h2>
      <p class="egcta__desc animate-on-scroll" data-animation="fadeInUp">See how gamification drives attendee engagement and gives sponsors measurable ROI.</p>
      <div class="egcta__proof-row animate-on-scroll" data-animation="fadeInUp">
        <span class="egcta__proof-pill">3x engagement boost</span>
        <span class="egcta__proof-pill">QR-verified data</span>
        <span class="egcta__proof-pill">Free onboarding support</span>
      </div>
      <div class="egcta__buttons">
        <a href="/book-a-demo/" class="egcta__btn egcta__btn--primary">
          Book a Free Demo
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M6 3l5 5-5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </a>
        <a href="/contact-en/" class="egcta__btn egcta__btn--secondary">Contact Sales</a>
      </div>
      <div class="egcta__trust">
        <span class="egcta__trust-item">No credit card required</span>
        <span class="egcta__trust-item">Free onboarding support</span>
        <span class="egcta__trust-item">Cancel anytime</span>
      </div>
    </div>
  </div>
</section>

<style is:global>
  .egcta { max-width: 1200px; margin: 0 auto; padding: 60px 20px 80px; }

  .egcta__card {
    position: relative;
    border-radius: 24px;
    padding: 80px 40px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    overflow: hidden;
    background: #0F1629;
  }

  .egcta__glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    pointer-events: none;
  }

  .egcta__glow--1 {
    width: 400px;
    height: 400px;
    background: rgba(55, 93, 251, 0.2);
    top: -100px;
    left: -50px;
  }

  .egcta__glow--2 {
    width: 300px;
    height: 300px;
    background: rgba(159, 129, 255, 0.15);
    bottom: -80px;
    right: -30px;
  }

  .egcta__content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
    max-width: 600px;
  }

  .egcta__eyebrow {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 8px 14px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.12);
    font-family: var(--font-body);
    font-size: 13px;
    font-weight: 600;
    line-height: 18px;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.72);
  }

  .egcta__title {
    font-family: var(--font-heading);
    font-size: 48px;
    font-weight: 600;
    line-height: 1.15;
    letter-spacing: -1px;
    color: #fff;
    margin: 0;
  }

  .egcta__desc {
    font-family: var(--font-body);
    font-size: 18px;
    font-weight: 400;
    line-height: 28px;
    letter-spacing: -0.3px;
    color: rgba(255, 255, 255, 0.65);
    margin: 0;
  }

  .egcta__proof-row {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 10px;
  }

  .egcta__proof-pill {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 9px 12px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.08);
    font-family: var(--font-body);
    font-size: 13px;
    font-weight: 500;
    line-height: 18px;
    color: rgba(255, 255, 255, 0.74);
  }

  .egcta__buttons { display: flex; gap: 12px; justify-content: center; }

  .egcta__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    font-family: var(--font-body);
    font-size: 15px;
    font-weight: 600;
    line-height: 24px;
    letter-spacing: -0.3px;
    padding: 12px 28px;
    border-radius: 10px;
    text-decoration: none;
    transition: background 0.3s, transform 0.3s, box-shadow 0.3s;
  }

  .egcta__btn--primary {
    background: var(--brand-blue);
    color: #fff;
    box-shadow: 0 4px 20px rgba(55, 93, 251, 0.35);
  }

  .egcta__btn--primary:hover {
    background: var(--brand-blue-hover);
    transform: translateY(-1px);
    box-shadow: 0 6px 28px rgba(55, 93, 251, 0.4);
  }

  .egcta__btn--secondary {
    background: rgba(255, 255, 255, 0.08);
    color: #fff;
    border: 1px solid rgba(255, 255, 255, 0.12);
  }

  .egcta__btn--secondary:hover {
    background: rgba(255, 255, 255, 0.15);
  }

  .egcta__trust {
    display: flex;
    gap: 20px;
    align-items: center;
    margin-top: 8px;
  }

  .egcta__trust-item {
    position: relative;
    padding-left: 14px;
    font-family: var(--font-body);
    font-size: 13px;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.45);
  }

  .egcta__trust-item::before {
    content: "";
    position: absolute;
    left: 0;
    top: 6px;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.45);
  }

  @media (max-width: 1024px) {
    .egcta__card { padding: 60px 24px; }
    .egcta__title { font-size: 36px; }
    .egcta__desc { font-size: 16px; }
  }

  @media (max-width: 767px) {
    .egcta { padding: 40px 16px 60px; }
    .egcta__card { padding: 48px 20px; border-radius: 20px; }
    .egcta__title { font-size: 28px; line-height: 1.2em; }
    .egcta__desc { font-size: 15px; }
    .egcta__proof-row { gap: 8px; }
    .egcta__buttons { flex-direction: column; width: 100%; }
    .egcta__btn { width: 100%; justify-content: center; }
    .egcta__trust { flex-direction: column; gap: 8px; }
  }
</style>
