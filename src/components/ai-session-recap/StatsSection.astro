---
const stats = [
  { value: "10K+", label: "Sessions Recapped" },
  { value: "90%", label: "Time Saved" },
  { value: "25+", label: "Languages Supported" },
  { value: "4.8/5", label: "Organizer Rating" },
];
---

<section class="asrs-dark">
  <div class="asrs-engagement">
    <div class="asrs-engagement-text">
      <h2 class="asrs-heading animate-on-scroll" data-animation="fadeInDown">
        <span class="asrs-white">Capture session knowledge with </span>
        <span class="asrs-purple">AI transcription </span>
        <span class="asrs-white">and </span>
        <span class="asrs-cyan">automated recap delivery</span>
      </h2>
    </div>
  </div>

  <div class="asrs-stats">
    {stats.map((stat) => (
      <div class="asrs-stat">
        <span class="asrs-stat-value">{stat.value}</span>
        <span class="asrs-stat-label">{stat.label}</span>
      </div>
    ))}
  </div>
</section>

<style>
  .asrs-dark {
    display: flex;
    flex-direction: column;
    width: 100%;
    padding: 110px 0;
    background-color: #000;
    background-image:
      radial-gradient(at 57% 33%, hsl(219 0% 0%) 0px, transparent 50%),
      radial-gradient(at 46% 60%, hsl(219 100% 15%) 0px, transparent 50%),
      radial-gradient(at 60% 56%, hsl(327 100% 11%) 0px, transparent 50%),
      radial-gradient(at 49% 0%, hsl(307 100% 11%) 0px, transparent 50%),
      radial-gradient(at 96% 0%, hsl(195 97% 13%) 0px, transparent 50%),
      radial-gradient(at 96% 97%, hsl(206 100% 24%) 0px, transparent 50%),
      radial-gradient(at 8% 82%, hsl(293 96% 10%) 0px, transparent 50%),
      radial-gradient(at 0% 0%, hsl(196 100% 21%) 0px, transparent 50%),
      url('https://cms.eventhex.ai/wp-content/uploads/2024/12/bg-1.webp');
    background-position: center center;
    background-repeat: no-repeat;
    background-size: cover;
  }

  .asrs-engagement {
    width: 100%;
    padding: 0 0 60px;
    display: flex;
    justify-content: center;
  }

  .asrs-engagement-text {
    width: 65%;
  }

  .asrs-heading {
    font-family: var(--font-heading);
    font-size: 48px;
    font-weight: 400;
    line-height: 52px;
    letter-spacing: -1px;
    color: #fff;
    text-align: center;
    margin: 0;
  }

  .asrs-white { color: #fff; }
  .asrs-purple { color: #AFA3FF; }
  .asrs-cyan { color: #ABFFFF; }

  .asrs-stats {
    display: flex;
    justify-content: center;
    gap: 60px;
    padding: 0 20px;
    max-width: 1100px;
    margin: 0 auto;
    width: 100%;
  }

  .asrs-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
  }

  .asrs-stat-value {
    font-family: var(--font-heading);
    font-size: 40px;
    font-weight: 700;
    line-height: 48px;
    letter-spacing: -1px;
    color: #fff;
  }

  .asrs-stat-label {
    font-family: var(--font-body);
    font-size: 15px;
    font-weight: 400;
    line-height: 20px;
    color: rgba(255, 255, 255, 0.6);
  }

  @media (max-width: 1024px) {
    .asrs-dark { padding: 80px 0; }
    .asrs-engagement { padding: 0 20px 40px; }
    .asrs-engagement-text { width: 85%; }
    .asrs-heading { font-size: 40px; line-height: 1.1em; }
    .asrs-stats { gap: 40px; }
    .asrs-stat-value { font-size: 32px; }
    .asrs-stat-label { font-size: 14px; }
  }

  @media (max-width: 767px) {
    .asrs-dark { padding: 64px 0; }
    .asrs-engagement { padding: 0 15px 30px; }
    .asrs-engagement-text { width: 100%; }
    .asrs-heading { font-size: 25px; line-height: 1.2em; letter-spacing: 0; }
    .asrs-stats {
      flex-wrap: wrap;
      gap: 24px;
      justify-content: center;
    }
    .asrs-stat { width: 40%; }
    .asrs-stat-value { font-size: 28px; }
    .asrs-stat-label { font-size: 13px; }
  }
</style>
