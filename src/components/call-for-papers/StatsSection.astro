---
const stats = [
  { value: "Multi-Round", label: "Review Support" },
  { value: "Custom", label: "Review Rubrics" },
  { value: "Auto", label: "Acceptance Emails" },
  { value: "100%", label: "Blind Review Support" },
];
---

<section class="cfps-dark">
  <div class="cfps-engagement">
    <div class="cfps-engagement-text">
      <h2 class="cfps-heading animate-on-scroll" data-animation="fadeInDown">
        <span class="cfps-white">Manage submissions with </span>
        <span class="cfps-purple">peer review </span>
        <span class="cfps-white">and </span>
        <span class="cfps-cyan">rubric-based scoring</span>
      </h2>
    </div>
  </div>

  <div class="cfps-stats">
    {stats.map((stat) => (
      <div class="cfps-stat">
        <span class="cfps-stat-value">{stat.value}</span>
        <span class="cfps-stat-label">{stat.label}</span>
      </div>
    ))}
  </div>
</section>

<style>
  .cfps-dark {
    display: flex;
    flex-direction: column;
    width: 100%;
    padding: 110px 0;
    background-color: #000;
    background-image:
      radial-gradient(at 57% 33%, hsl(219 0% 0%) 0px, transparent 50%),
      radial-gradient(at 46% 60%, hsl(219 100% 15%) 0px, transparent 50%),
      radial-gradient(at 60% 56%, hsl(327 100% 11%) 0px, transparent 50%),
      radial-gradient(at 49% 0%, hsl(307 100% 11%) 0px, transparent 50%),
      radial-gradient(at 96% 0%, hsl(195 97% 13%) 0px, transparent 50%),
      radial-gradient(at 96% 97%, hsl(206 100% 24%) 0px, transparent 50%),
      radial-gradient(at 8% 82%, hsl(293 96% 10%) 0px, transparent 50%),
      radial-gradient(at 0% 0%, hsl(196 100% 21%) 0px, transparent 50%),
      url('https://eventhex.ai/wp-content/uploads/2024/12/bg-1.webp');
    background-position: center center;
    background-repeat: no-repeat;
    background-size: cover;
  }

  .cfps-engagement {
    width: 100%;
    padding: 0 0 60px;
    display: flex;
    justify-content: center;
  }

  .cfps-engagement-text {
    width: 65%;
  }

  .cfps-heading {
    font-family: var(--font-heading);
    font-size: 48px;
    font-weight: 400;
    line-height: 52px;
    letter-spacing: -1px;
    color: #fff;
    text-align: center;
    margin: 0;
  }

  .cfps-white { color: #fff; }
  .cfps-purple { color: #AFA3FF; }
  .cfps-cyan { color: #ABFFFF; }

  .cfps-stats {
    display: flex;
    justify-content: center;
    gap: 60px;
    padding: 0 20px;
    max-width: 1100px;
    margin: 0 auto;
    width: 100%;
  }

  .cfps-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
  }

  .cfps-stat-value {
    font-family: var(--font-heading);
    font-size: 40px;
    font-weight: 700;
    line-height: 48px;
    letter-spacing: -1px;
    color: #fff;
  }

  .cfps-stat-label {
    font-family: var(--font-body);
    font-size: 15px;
    font-weight: 400;
    line-height: 20px;
    color: rgba(255, 255, 255, 0.6);
  }

  @media (max-width: 1024px) {
    .cfps-dark { padding: 80px 0; }
    .cfps-engagement { padding: 0 20px 40px; }
    .cfps-engagement-text { width: 85%; }
    .cfps-heading { font-size: 40px; line-height: 1.1em; }
    .cfps-stats { gap: 40px; }
    .cfps-stat-value { font-size: 32px; }
    .cfps-stat-label { font-size: 14px; }
  }

  @media (max-width: 767px) {
    .cfps-dark { padding: 64px 0; }
    .cfps-engagement { padding: 0 15px 30px; }
    .cfps-engagement-text { width: 100%; }
    .cfps-heading { font-size: 25px; line-height: 1.2em; letter-spacing: 0; }
    .cfps-stats {
      flex-wrap: wrap;
      gap: 24px;
      justify-content: center;
    }
    .cfps-stat { width: 40%; }
    .cfps-stat-value { font-size: 28px; }
    .cfps-stat-label { font-size: 13px; }
  }
</style>
