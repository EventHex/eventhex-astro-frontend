<section class="eppx-hero">
  <div class="eppx-hero__grid">
    <div class="eppx-hero__content">
      <span class="eppx-hero__badge animate-on-scroll" data-animation="fadeInDown">
        Event payment processing
      </span>
      <h1 class="eppx-hero__title animate-on-scroll" data-animation="fadeInUp">
        Connect ticket sales to the payment setup your event uses
      </h1>
      <p class="eppx-hero__desc animate-on-scroll" data-animation="fadeInUp" style="--anim-duration: 800ms">
        Route paid checkout through Stripe, Razorpay, or Tap, use your supported
        gateway account, and keep payment, refund, tax, and estimated net-payout
        context connected to the event order.
      </p>
      <div class="eppx-hero__points animate-on-scroll" data-animation="fadeInUp" style="--anim-duration: 950ms">
        <span>Stripe, Razorpay, and Tap</span>
        <span>Order-linked payment state</span>
        <span>Event-level finance view</span>
      </div>
      <div class="eppx-hero__cta-row animate-on-scroll" data-animation="fadeInUp" style="--anim-duration: 1100ms">
        <a href="/book-a-demo/" class="eppx-hero__btn">
          Book a Demo
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
            <path d="M6 3l5 5-5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path>
          </svg>
        </a>
        <a href="#payment-flow" class="eppx-hero__btn eppx-hero__btn--outline">
          See the payment flow
        </a>
      </div>
    </div>

    <div class="eppx-hero__visual animate-on-scroll" data-animation="fadeInRight">
      <div class="eppx-hero__visual-card">
        <div class="eppx-hero__visual-inner" aria-hidden="true">
          <div class="eppx-hero__mockup">
            <div class="eppx-hero__mockup-rail">
              <i></i><i></i><i></i><i></i>
            </div>
            <div class="eppx-hero__mockup-main">
              <div class="eppx-hero__mockup-top"><i></i><i></i></div>
              <div class="eppx-hero__mockup-field"><i></i><i></i></div>
              <div class="eppx-hero__mockup-field eppx-hero__mockup-field--short"><i></i><i></i></div>
              <div class="eppx-hero__mockup-options"><i></i><i></i><i></i></div>
              <div class="eppx-hero__mockup-summary"><i></i><i></i></div>
              <i class="eppx-hero__mockup-button"></i>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .eppx-hero {
    position: relative;
    overflow: hidden;
    margin-top: -90px;
    padding: 140px 24px 64px;
    background:
      radial-gradient(ellipse 700px 500px at -5% -10%, #375dfb24, #375dfb0f 40%, #375dfb00 75%),
      radial-gradient(ellipse 800px 600px at 100% 105%, #ff74b83d, #ff74b81a 45%, #ff74b800 78%),
      #fff;
  }

  .eppx-hero::after {
    content: "";
    position: absolute;
    inset: 0 0 0 auto;
    z-index: 2;
    width: 220px;
    max-width: 30vw;
    background: linear-gradient(to right, #fff0, #ffffff73 70%, #ffffffbf);
    pointer-events: none;
  }

  .eppx-hero__grid {
    position: relative;
    z-index: 1;
    max-width: 1080px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: minmax(260px, 542px) minmax(0, 490px);
    gap: 48px;
    align-items: center;
  }

  .eppx-hero__content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 20px;
  }

  .eppx-hero__badge {
    color: var(--brand-blue);
    font: 500 16px/1.4 var(--font-body);
    text-transform: uppercase;
  }

  .eppx-hero__title {
    width: 100%;
    margin: 0;
    font-family: var(--font-heading);
    font-size: clamp(44px, 5vw, 60px);
    font-weight: 500;
    line-height: 1.1;
    letter-spacing: -2.1px;
    color: transparent;
    background: linear-gradient(98deg, var(--heading-dark) 43.06%, #8f8f8f 91.81%);
    background-clip: text;
    -webkit-background-clip: text;
  }

  .eppx-hero__desc {
    width: 100%;
    margin: 0;
    color: var(--subheading-gray);
    font: 400 18px/24px var(--font-body);
    letter-spacing: -0.25px;
  }

  .eppx-hero__points {
    display: flex;
    flex-wrap: wrap;
    gap: 10px 18px;
  }

  .eppx-hero__points span {
    position: relative;
    padding-left: 16px;
    color: #475467;
    font: 500 14px/20px var(--font-body);
  }

  .eppx-hero__points span::before {
    content: "";
    position: absolute;
    left: 0;
    top: 7px;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #10b981;
  }

  .eppx-hero__cta-row {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 16px;
    padding-top: 8px;
  }

  .eppx-hero__btn {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 12px 20px;
    border: 1px solid transparent;
    border-radius: 12px;
    background: var(--heading-dark);
    color: #fff;
    font: 650 15px/22px var(--font-body);
    letter-spacing: -0.3px;
    text-decoration: none;
    transition: transform 0.3s, background 0.3s, border-color 0.3s;
  }

  .eppx-hero__btn:hover {
    transform: translateY(-2px);
    background: #000;
  }

  .eppx-hero__btn--outline {
    border-color: rgba(16, 24, 40, 0.22);
    background: transparent;
    color: var(--heading-dark);
  }

  .eppx-hero__btn--outline:hover {
    border-color: var(--heading-dark);
    background: transparent;
  }

  .eppx-hero__visual {
    position: relative;
    margin-right: calc(-1 * (max(0px, (100vw - 1128px) / 2) + 24px));
  }

  .eppx-hero__visual-card {
    position: relative;
    padding: 14px;
    border: 1px solid rgba(255, 255, 255, 0.62);
    border-right: 0;
    border-radius: 22px 0 0 22px;
    background: linear-gradient(180deg, #ffffff9e, #ffffff47), #ffffff57;
    backdrop-filter: blur(16px);
  }

  .eppx-hero__visual-inner {
    position: relative;
    overflow: hidden;
    padding: 28px;
    border-radius: 16px 0 0 16px;
    background: linear-gradient(135deg, #d9ecfa, #eef6fc 45%, #fff);
  }

  .eppx-hero__mockup {
    display: grid;
    grid-template-columns: 64px 1fr;
    min-height: 430px;
    overflow: hidden;
    border: 1px solid rgba(55, 93, 251, .13);
    border-radius: 18px;
    background: #fff;
    box-shadow: 0 18px 34px rgba(31, 52, 128, .12);
  }

  .eppx-hero__mockup-rail {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 18px;
    padding: 24px 12px;
    background: #f6f8ff;
  }

  .eppx-hero__mockup-rail i {
    width: 24px;
    height: 24px;
    border-radius: 8px;
    background: #d9e1ff;
  }

  .eppx-hero__mockup-rail i:first-child {
    width: 34px;
    height: 34px;
    margin-bottom: 12px;
    border-radius: 11px;
    background: linear-gradient(135deg, #375dfb, #7c8fff);
  }

  .eppx-hero__mockup-main {
    display: flex;
    flex-direction: column;
    gap: 18px;
    padding: 34px 30px;
  }

  .eppx-hero__mockup-top,
  .eppx-hero__mockup-field,
  .eppx-hero__mockup-summary {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .eppx-hero__mockup-top i:first-child { width: 42%; height: 15px; border-radius: 6px; background: #d9deea; }
  .eppx-hero__mockup-top i:last-child { width: 32px; height: 32px; border-radius: 50%; background: #e8ecf5; }

  .eppx-hero__mockup-field {
    min-height: 62px;
    padding: 14px 16px;
    border: 1px solid #e5e9f2;
    border-radius: 12px;
  }
  .eppx-hero__mockup-field i:first-child { width: 36%; height: 9px; border-radius: 5px; background: #dfe4ed; }
  .eppx-hero__mockup-field i:last-child { width: 24%; height: 9px; border-radius: 5px; background: #edf0f5; }
  .eppx-hero__mockup-field--short { width: 70%; }

  .eppx-hero__mockup-options {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
  }
  .eppx-hero__mockup-options i {
    height: 66px;
    border: 1px solid #e1e6f0;
    border-radius: 12px;
    background: linear-gradient(145deg, #fff, #f4f6fb);
  }
  .eppx-hero__mockup-options i:first-child {
    border-color: rgba(55, 93, 251, .45);
    box-shadow: inset 0 0 0 2px rgba(55, 93, 251, .08);
  }

  .eppx-hero__mockup-summary {
    padding-top: 8px;
    border-top: 1px solid #edf0f5;
  }
  .eppx-hero__mockup-summary i:first-child { width: 30%; height: 10px; border-radius: 5px; background: #dfe4ed; }
  .eppx-hero__mockup-summary i:last-child { width: 18%; height: 16px; border-radius: 5px; background: #b9c3d8; }

  .eppx-hero__mockup-button {
    width: 54%;
    height: 42px;
    margin-top: auto;
    border-radius: 10px;
    background: linear-gradient(90deg, #375dfb, #6c82ff);
  }

  @media (max-width: 960px) {
    .eppx-hero__grid { grid-template-columns: 1fr; }
    .eppx-hero__content { max-width: 720px; margin: 0 auto; align-items: center; text-align: center; }
    .eppx-hero__points, .eppx-hero__cta-row { justify-content: center; }
    .eppx-hero__visual { width: min(680px, 100%); margin: 0 auto; }
    .eppx-hero__visual-card, .eppx-hero__visual-inner {
      border-radius: 22px;
      border-right: 1px solid rgba(255, 255, 255, 0.62);
    }
  }

  @media (max-width: 767px) {
    .eppx-hero { margin-top: -80px; padding: 120px 20px 52px; }
    .eppx-hero::after { display: none; }
    .eppx-hero__grid { gap: 34px; }
    .eppx-hero__title { font-size: 38px; line-height: 1.08; letter-spacing: -1.2px; }
    .eppx-hero__desc { font-size: 16px; line-height: 1.6; }
    .eppx-hero__cta-row { width: 100%; flex-direction: column; }
    .eppx-hero__btn { width: 100%; justify-content: center; }
    .eppx-hero__visual-inner { padding: 18px 10px 10px; }
    .eppx-hero__mockup { grid-template-columns: 48px 1fr; min-height: 340px; }
    .eppx-hero__mockup-rail { gap: 14px; padding: 18px 8px; }
    .eppx-hero__mockup-rail i { width: 20px; height: 20px; }
    .eppx-hero__mockup-rail i:first-child { width: 28px; height: 28px; }
    .eppx-hero__mockup-main { gap: 13px; padding: 24px 18px; }
    .eppx-hero__mockup-field { min-height: 48px; padding: 10px 12px; }
    .eppx-hero__mockup-options i { height: 50px; }
  }
</style>
