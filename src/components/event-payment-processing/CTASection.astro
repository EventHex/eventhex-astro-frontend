<section class="eppx-cta">
  <div class="eppx-cta__card animate-on-scroll" data-animation="fadeInUp">
    <span>Map your event payment setup</span>
    <h2>Bring your gateway and finance workflow to the demo</h2>
    <p>
      Show us the market, currency, ticket flow, supported provider account,
      refund process, and reporting needs for one event. We’ll walk through the
      matching EventHex path and call out anything that requires separate scoping.
    </p>
    <a href="/book-a-demo/">
      Book a Demo
      <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
        <path d="M6 3l5 5-5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path>
      </svg>
    </a>
  </div>
</section>

<style>
  .eppx-cta {
    padding: 24px 24px 96px;
    background: #fff;
  }
  .eppx-cta__card {
    position: relative;
    isolation: isolate;
    max-width: 1140px;
    min-height: 430px;
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    padding: 64px;
    border-radius: 24px;
    background:
      radial-gradient(circle at 15% 15%, rgba(75, 126, 255, .3), transparent 30%),
      radial-gradient(circle at 88% 82%, rgba(169, 113, 255, .22), transparent 34%),
      #090b12;
    color: #fff;
    text-align: center;
  }
  .eppx-cta__card::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: -1;
    opacity: .28;
    background-image: radial-gradient(rgba(255,255,255,.34) .7px, transparent .7px);
    background-size: 24px 24px;
  }
  .eppx-cta__card > span {
    margin-bottom: 18px;
    color: #afa3ff;
    font: 650 13px/1.3 var(--font-body);
    letter-spacing: .08em;
    text-transform: uppercase;
  }
  .eppx-cta__card h2 {
    max-width: 850px;
    margin: 0;
    color: #fff;
    font: 500 clamp(38px, 5vw, 60px)/1.04 var(--font-heading);
    letter-spacing: -.045em;
  }
  .eppx-cta__card p {
    max-width: 760px;
    margin: 22px auto 30px;
    color: rgba(255,255,255,.62);
    font: 400 17px/1.65 var(--font-body);
  }
  .eppx-cta__card a {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 14px 24px;
    border-radius: 12px;
    background: #fff;
    color: #111827;
    font: 650 15px/1.4 var(--font-body);
    text-decoration: none;
    transition: transform .25s ease, box-shadow .25s ease;
  }
  .eppx-cta__card a:hover { transform: translateY(-2px); box-shadow: 0 14px 30px rgba(0,0,0,.22); }

  @media (max-width: 767px) {
    .eppx-cta { padding: 8px 16px 72px; }
    .eppx-cta__card { min-height: 500px; padding: 48px 24px; }
    .eppx-cta__card h2 { font-size: 38px; }
    .eppx-cta__card p { font-size: 15px; }
    .eppx-cta__card a { width: 100%; justify-content: center; }
  }
</style>
