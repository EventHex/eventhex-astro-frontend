<section class="eppx-evidence" aria-labelledby="evidence-heading">
  <div class="eppx-evidence__shell">
    <header class="eppx-evidence__header animate-on-scroll" data-animation="fadeInUp">
      <span>Refund and finance operations</span>
      <h2 id="evidence-heading">Keep the financial outcome beside the original event order</h2>
    </header>

    <div class="eppx-evidence__grid">
      <div class="eppx-evidence__row">
        <article class="eppx-evidence__copy animate-on-scroll" data-animation="fadeInLeft">
          <span>Organizer-managed refunds</span>
          <h3>Process a full or partial refund from the order context</h3>
          <p>
            Let an authorized organizer choose the amount, send the request through
            the supported provider path, and retain the history beside the original payment.
          </p>
          <ul>
            <li><b>Full refund</b><span>Return the remaining eligible value for the order.</span></li>
            <li><b>Partial refund</b><span>Refund a smaller amount without exceeding the paid balance.</span></li>
            <li><b>Remaining-balance guard</b><span>Cap another refund at the order total minus earlier refunds.</span></li>
            <li><b>Attendee notification</b><span>Use the configured refund communication after the organizer action.</span></li>
          </ul>
        </article>
        <div class="eppx-evidence__media animate-on-scroll" data-animation="fadeInRight">
          <img
            src="/feature-images/event-payment-processing/refunds.svg"
            alt="EventHex refund view showing a refund list and order-level refund amounts"
            width="617"
            height="460"
            loading="lazy"
            decoding="async"
          />
          <span class="eppx-evidence__badge"><i></i> Refund history retained</span>
        </div>
      </div>

      <div class="eppx-evidence__row">
        <div class="eppx-evidence__media animate-on-scroll" data-animation="fadeInLeft">
          <img
            src="/feature-images/event-payment-processing/revenue-reporting.svg"
            alt="EventHex order detail and finance reporting view with event revenue context"
            width="797"
            height="476"
            loading="lazy"
            decoding="async"
          />
          <span class="eppx-evidence__badge eppx-evidence__badge--blue"><i></i> Event finance view</span>
        </div>
        <article class="eppx-evidence__copy animate-on-scroll" data-animation="fadeInRight">
          <span>Event finance view</span>
          <h3>Review revenue and refund context by currency, status, and gateway</h3>
          <p>
            Move from individual orders to an operational summary for the finance team.
          </p>
          <ul>
            <li><b>Gross collected</b><span>Review captured value for the selected event context.</span></li>
            <li><b>Refunded</b><span>See returned amounts without treating gross sales as retained revenue.</span></li>
            <li><b>Fees and tax</b><span>Keep estimated gateway fees, platform-fee context, and recorded tax separate.</span></li>
            <li><b>Estimated net payout</b><span>Review the calculated event view before comparing it with processor records.</span></li>
          </ul>
        </article>
      </div>
    </div>
  </div>
</section>

<style>
  .eppx-evidence {
    padding: 108px 24px;
    background:
      radial-gradient(circle at 8% 4%, rgba(55,93,251,.07), transparent 24%),
      #fff;
  }

  .eppx-evidence__shell {
    max-width: 1140px;
    margin: 0 auto;
  }

  .eppx-evidence__header {
    max-width: 850px;
    margin: 0 auto 42px;
    text-align: center;
  }
  .eppx-evidence__header > span,
  .eppx-evidence__copy > span {
    color: #375dfb;
    font: 650 13px/1.3 var(--font-body);
    letter-spacing: .08em;
    text-transform: uppercase;
  }
  .eppx-evidence__header h2 {
    margin: 12px 0 0;
    color: var(--heading-dark);
    font: 500 clamp(36px, 4.6vw, 56px)/1.05 var(--font-heading);
    letter-spacing: -.045em;
  }

  .eppx-evidence__grid {
    overflow: hidden;
    border: 1px solid #e5e7eb;
    border-radius: 24px;
    background: #fff;
  }

  .eppx-evidence__row {
    display: grid;
    grid-template-columns: 1fr 1fr;
  }
  .eppx-evidence__row + .eppx-evidence__row { border-top: 1px solid #e5e7eb; }
  .eppx-evidence__row > *:first-child { border-right: 1px solid #e5e7eb; }

  .eppx-evidence__copy {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 58px 48px;
  }
  .eppx-evidence__copy h3 {
    margin: 14px 0 16px;
    color: var(--heading-dark);
    font: 500 34px/1.12 var(--font-heading);
    letter-spacing: -.035em;
  }
  .eppx-evidence__copy > p {
    margin: 0;
    color: var(--subheading-gray);
    font: 400 16px/1.65 var(--font-body);
  }
  .eppx-evidence__copy ul {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
    margin: 26px 0 0;
    padding: 0;
    list-style: none;
  }
  .eppx-evidence__copy li {
    padding: 13px;
    border: 1px solid #e8edf5;
    border-radius: 12px;
    background: #fbfcff;
  }
  .eppx-evidence__copy li b,
  .eppx-evidence__copy li span { display: block; font-family: var(--font-body); }
  .eppx-evidence__copy li b { color: #344054; font-size: 12px; }
  .eppx-evidence__copy li span { margin-top: 4px; color: #7a8495; font-size: 10px; line-height: 1.45; }

  .eppx-evidence__media {
    position: relative;
    min-height: 570px;
    display: grid;
    place-items: center;
    overflow: hidden;
    padding: 44px;
    background: linear-gradient(145deg, #f2f5ff, #fff);
  }
  .eppx-evidence__media::before {
    content: "";
    position: absolute;
    width: 310px;
    height: 310px;
    border-radius: 50%;
    background: rgba(55,93,251,.09);
    filter: blur(8px);
  }
  .eppx-evidence__media img {
    position: relative;
    z-index: 1;
    display: block;
    width: 108%;
    max-width: none;
    height: auto;
    filter: drop-shadow(0 22px 40px rgba(31,52,128,.14));
  }

  .eppx-evidence__badge {
    position: absolute;
    z-index: 2;
    right: 24px;
    bottom: 24px;
    display: inline-flex;
    align-items: center;
    gap: 9px;
    padding: 11px 14px;
    border: 1px solid #e2e7f0;
    border-radius: 12px;
    background: rgba(255,255,255,.94);
    color: #344054;
    box-shadow: 0 14px 34px rgba(31,52,128,.14);
    font: 650 11px/1.3 var(--font-body);
  }
  .eppx-evidence__badge i { width: 8px; height: 8px; border-radius: 50%; background: #10b981; }
  .eppx-evidence__badge--blue i { background: #375dfb; }

  @media (max-width: 960px) {
    .eppx-evidence__row { grid-template-columns: 1fr; }
    .eppx-evidence__row > *:first-child { border-right: 0; }
    .eppx-evidence__row > * + * { border-top: 1px solid #e5e7eb; }
    .eppx-evidence__row:nth-child(2) .eppx-evidence__copy { grid-row: 1; }
    .eppx-evidence__row:nth-child(2) .eppx-evidence__media { grid-row: 2; }
    .eppx-evidence__media { min-height: 480px; }
  }

  @media (max-width: 767px) {
    .eppx-evidence { padding: 72px 16px; }
    .eppx-evidence__header h2 { font-size: 34px; }
    .eppx-evidence__copy { padding: 38px 24px; }
    .eppx-evidence__copy h3 { font-size: 28px; }
    .eppx-evidence__copy ul { grid-template-columns: 1fr; }
    .eppx-evidence__media { min-height: 330px; padding: 24px 12px; }
    .eppx-evidence__media img { width: 112%; }
    .eppx-evidence__badge { right: 12px; bottom: 12px; }
  }
</style>
