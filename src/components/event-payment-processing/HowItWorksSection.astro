<section class="eppx-evidence">
  <div class="eppx-evidence__shell">
    <div class="eppx-evidence__grid">
      <div class="eppx-evidence__row">
        <article class="eppx-evidence__copy animate-on-scroll" data-animation="fadeInLeft">
          <span>Organizer-managed refunds</span>
          <h3>Process a full or partial refund from the order context</h3>
          <p>
            Let an authorized organizer choose the amount, send the request through
            the supported provider path, and retain the history beside the original payment.
          </p>
          <ul>
            <li><b>Full refund</b><span>Return the remaining eligible value for the order.</span></li>
            <li><b>Partial refund</b><span>Refund a smaller amount without exceeding the paid balance.</span></li>
            <li><b>Remaining-balance guard</b><span>Cap another refund at the order total minus earlier refunds.</span></li>
            <li><b>Attendee notification</b><span>Use the configured refund communication after the organizer action.</span></li>
          </ul>
        </article>
        <div class="eppx-evidence__media animate-on-scroll" data-animation="fadeInRight" aria-hidden="true">
          <div class="eppx-evidence__mockup eppx-evidence__mockup--refund">
            <div class="eppx-evidence__mockup-head"><i></i><i></i></div>
            <div class="eppx-evidence__mockup-tabs"><i></i><i></i><i></i></div>
            <div class="eppx-evidence__mockup-list">
              <span><i></i><i></i><i></i></span>
              <span><i></i><i></i><i></i></span>
              <span><i></i><i></i><i></i></span>
              <span><i></i><i></i><i></i></span>
            </div>
          </div>
        </div>
      </div>

      <div class="eppx-evidence__row">
        <div class="eppx-evidence__media animate-on-scroll" data-animation="fadeInLeft" aria-hidden="true">
          <div class="eppx-evidence__mockup eppx-evidence__mockup--finance">
            <div class="eppx-evidence__mockup-head"><i></i><i></i></div>
            <div class="eppx-evidence__mockup-metrics"><i></i><i></i><i></i></div>
            <div class="eppx-evidence__mockup-chart">
              <i></i><i></i><i></i><i></i><i></i><i></i><i></i>
            </div>
          </div>
        </div>
        <article class="eppx-evidence__copy animate-on-scroll" data-animation="fadeInRight">
          <span>Event finance view</span>
          <h3>Review revenue and refund context by currency, status, and gateway</h3>
          <p>
            Use the event-level finance view to move from individual orders to an operational summary for the finance team.
          </p>
          <ul>
            <li><b>Gross collected</b><span>Review captured value for the selected event context.</span></li>
            <li><b>Refunded</b><span>See returned amounts without treating gross sales as retained revenue.</span></li>
            <li><b>Fees and tax</b><span>Keep estimated gateway fees, platform-fee context, and recorded tax visible as separate fields.</span></li>
            <li><b>Estimated net payout</b><span>Review the calculated event view by currency, status, gateway, and provider before comparing it with processor records.</span></li>
          </ul>
        </article>
      </div>
    </div>
  </div>
</section>

<style>
  .eppx-evidence {
    padding: 108px 24px;
    background:
      radial-gradient(circle at 8% 4%, rgba(55,93,251,.07), transparent 24%),
      #fff;
  }

  .eppx-evidence__shell {
    max-width: 1140px;
    margin: 0 auto;
  }

  .eppx-evidence__copy > span {
    color: #375dfb;
    font: 650 13px/1.3 var(--font-body);
    letter-spacing: .08em;
    text-transform: uppercase;
  }
  .eppx-evidence__grid {
    overflow: hidden;
    border: 1px solid #e5e7eb;
    border-radius: 24px;
    background: #fff;
  }

  .eppx-evidence__row {
    display: grid;
    grid-template-columns: 1fr 1fr;
  }
  .eppx-evidence__row + .eppx-evidence__row { border-top: 1px solid #e5e7eb; }
  .eppx-evidence__row > *:first-child { border-right: 1px solid #e5e7eb; }

  .eppx-evidence__copy {
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 58px 48px;
  }
  .eppx-evidence__copy h3 {
    margin: 14px 0 16px;
    color: var(--heading-dark);
    font: 500 34px/1.12 var(--font-heading);
    letter-spacing: -.035em;
  }
  .eppx-evidence__copy > p {
    margin: 0;
    color: var(--subheading-gray);
    font: 400 16px/1.65 var(--font-body);
  }
  .eppx-evidence__copy ul {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
    margin: 26px 0 0;
    padding: 0;
    list-style: none;
  }
  .eppx-evidence__copy li {
    padding: 13px;
    border: 1px solid #e8edf5;
    border-radius: 12px;
    background: #fbfcff;
  }
  .eppx-evidence__copy li b,
  .eppx-evidence__copy li span { display: block; font-family: var(--font-body); }
  .eppx-evidence__copy li b { color: #344054; font-size: 12px; }
  .eppx-evidence__copy li span { margin-top: 4px; color: #7a8495; font-size: 10px; line-height: 1.45; }

  .eppx-evidence__media {
    position: relative;
    min-height: 570px;
    display: grid;
    place-items: center;
    overflow: hidden;
    padding: 44px;
    background: linear-gradient(145deg, #f2f5ff, #fff);
  }
  .eppx-evidence__media::before {
    content: "";
    position: absolute;
    width: 310px;
    height: 310px;
    border-radius: 50%;
    background: rgba(55,93,251,.09);
    filter: blur(8px);
  }
  .eppx-evidence__mockup {
    position: relative;
    z-index: 1;
    width: min(520px, 108%);
    min-height: 390px;
    padding: 26px;
    border: 1px solid rgba(55,93,251,.13);
    border-radius: 18px;
    background: #fff;
    box-shadow: 0 22px 40px rgba(31,52,128,.14);
  }
  .eppx-evidence__mockup-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding-bottom: 22px;
    border-bottom: 1px solid #edf0f5;
  }
  .eppx-evidence__mockup-head i:first-child { width: 38%; height: 14px; border-radius: 6px; background: #d6dce8; }
  .eppx-evidence__mockup-head i:last-child { width: 34px; height: 34px; border-radius: 10px; background: #eef1f7; }
  .eppx-evidence__mockup-tabs {
    display: flex;
    gap: 8px;
    margin: 24px 0 18px;
  }
  .eppx-evidence__mockup-tabs i { width: 68px; height: 28px; border-radius: 14px; background: #f0f2f7; }
  .eppx-evidence__mockup-tabs i:first-child { background: #dce4ff; }
  .eppx-evidence__mockup-list {
    display: grid;
    gap: 10px;
  }
  .eppx-evidence__mockup-list span {
    display: grid;
    grid-template-columns: 38px 1fr 72px;
    align-items: center;
    gap: 12px;
    min-height: 58px;
    padding: 10px 12px;
    border: 1px solid #edf0f5;
    border-radius: 10px;
  }
  .eppx-evidence__mockup-list i:first-child { width: 38px; height: 38px; border-radius: 50%; background: #e5eaff; }
  .eppx-evidence__mockup-list i:nth-child(2) { width: 66%; height: 9px; border-radius: 5px; background: #dfe4ed; }
  .eppx-evidence__mockup-list i:last-child { width: 72px; height: 26px; border-radius: 13px; background: #eef1f7; }
  .eppx-evidence__mockup-metrics {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 10px;
    margin: 24px 0;
  }
  .eppx-evidence__mockup-metrics i {
    height: 72px;
    border: 1px solid #e9edf4;
    border-radius: 12px;
    background: linear-gradient(145deg, #fff, #f7f8fc);
  }
  .eppx-evidence__mockup-chart {
    height: 210px;
    display: flex;
    align-items: flex-end;
    justify-content: space-around;
    gap: 10px;
    padding: 22px 18px 14px;
    border: 1px solid #edf0f5;
    border-radius: 14px;
    background: repeating-linear-gradient(to bottom, transparent 0 41px, #eef1f6 42px);
  }
  .eppx-evidence__mockup-chart i {
    flex: 1;
    max-width: 32px;
    border-radius: 8px 8px 3px 3px;
    background: linear-gradient(180deg, #7f95ff, #375dfb);
  }
  .eppx-evidence__mockup-chart i:nth-child(1) { height: 32%; }
  .eppx-evidence__mockup-chart i:nth-child(2) { height: 55%; }
  .eppx-evidence__mockup-chart i:nth-child(3) { height: 44%; }
  .eppx-evidence__mockup-chart i:nth-child(4) { height: 78%; }
  .eppx-evidence__mockup-chart i:nth-child(5) { height: 62%; }
  .eppx-evidence__mockup-chart i:nth-child(6) { height: 88%; }
  .eppx-evidence__mockup-chart i:nth-child(7) { height: 70%; }

  @media (max-width: 960px) {
    .eppx-evidence__row { grid-template-columns: 1fr; }
    .eppx-evidence__row > *:first-child { border-right: 0; }
    .eppx-evidence__row > * + * { border-top: 1px solid #e5e7eb; }
    .eppx-evidence__row:nth-child(2) .eppx-evidence__copy { grid-row: 1; }
    .eppx-evidence__row:nth-child(2) .eppx-evidence__media { grid-row: 2; }
    .eppx-evidence__media { min-height: 480px; }
  }

  @media (max-width: 767px) {
    .eppx-evidence { padding: 72px 16px; }
    .eppx-evidence__copy { padding: 38px 24px; }
    .eppx-evidence__copy h3 { font-size: 28px; }
    .eppx-evidence__copy ul { grid-template-columns: 1fr; }
    .eppx-evidence__media { min-height: 330px; padding: 24px 12px; }
    .eppx-evidence__mockup { width: 100%; min-height: 300px; padding: 18px; }
    .eppx-evidence__mockup-list span { grid-template-columns: 32px 1fr 52px; }
    .eppx-evidence__mockup-list i:first-child { width: 32px; height: 32px; }
    .eppx-evidence__mockup-list i:last-child { width: 52px; }
    .eppx-evidence__mockup-chart { height: 160px; gap: 7px; padding-inline: 10px; }
  }
</style>
