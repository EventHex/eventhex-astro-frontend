---
---

<section class="emacta">
  <div class="emacta__card">
    <div class="emacta__glow emacta__glow--1"></div>
    <div class="emacta__glow emacta__glow--2"></div>
    <div class="emacta__content">
      <span class="emacta__eyebrow animate-on-scroll" data-animation="fadeIn">Launch Your Event App</span>
      <h2 class="emacta__title animate-on-scroll" data-animation="fadeInUp">Ready to Give Attendees a Mobile Experience They'll Use?</h2>
      <p class="emacta__desc animate-on-scroll" data-animation="fadeInUp">See how EventHex mobile app drives 85% attendee adoption with AI networking, live engagement, and branded experiences.</p>
      <div class="emacta__proof-row animate-on-scroll" data-animation="fadeInUp">
        <span class="emacta__proof-pill">AI networking</span>
        <span class="emacta__proof-pill">White-label branding</span>
        <span class="emacta__proof-pill">Free onboarding support</span>
      </div>
      <div class="emacta__buttons">
        <a href="/book-a-demo/" class="emacta__btn emacta__btn--primary">
          Book a Free Demo
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M6 3l5 5-5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </a>
        <a href="/contact-en/" class="emacta__btn emacta__btn--secondary">Contact Sales</a>
      </div>
      <div class="emacta__trust">
        <span class="emacta__trust-item">No credit card required</span>
        <span class="emacta__trust-item">Free onboarding support</span>
        <span class="emacta__trust-item">Cancel anytime</span>
      </div>
    </div>
  </div>
</section>

<style is:global>
  .emacta { max-width: 1200px; margin: 0 auto; padding: 60px 20px 80px; }

  .emacta__card {
    position: relative;
    border-radius: 24px;
    padding: 80px 40px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    overflow: hidden;
    background: #0F1629;
  }

  .emacta__glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    pointer-events: none;
  }

  .emacta__glow--1 {
    width: 400px;
    height: 400px;
    background: rgba(55, 93, 251, 0.2);
    top: -100px;
    left: -50px;
  }

  .emacta__glow--2 {
    width: 300px;
    height: 300px;
    background: rgba(159, 129, 255, 0.15);
    bottom: -80px;
    right: -30px;
  }

  .emacta__content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
    max-width: 600px;
  }

  .emacta__eyebrow {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 8px 14px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.12);
    font-family: var(--font-body);
    font-size: 13px;
    font-weight: 600;
    line-height: 18px;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.72);
  }

  .emacta__title {
    font-family: var(--font-heading);
    font-size: 48px;
    font-weight: 600;
    line-height: 1.15;
    letter-spacing: -1px;
    color: #fff;
    margin: 0;
  }

  .emacta__desc {
    font-family: var(--font-body);
    font-size: 18px;
    font-weight: 400;
    line-height: 28px;
    letter-spacing: -0.3px;
    color: rgba(255, 255, 255, 0.65);
    margin: 0;
  }

  .emacta__proof-row {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 10px;
  }

  .emacta__proof-pill {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 9px 12px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.08);
    font-family: var(--font-body);
    font-size: 13px;
    font-weight: 500;
    line-height: 18px;
    color: rgba(255, 255, 255, 0.74);
  }

  .emacta__buttons { display: flex; gap: 12px; justify-content: center; }

  .emacta__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    font-family: var(--font-body);
    font-size: 15px;
    font-weight: 600;
    line-height: 24px;
    letter-spacing: -0.3px;
    padding: 12px 28px;
    border-radius: 10px;
    text-decoration: none;
    transition: background 0.3s, transform 0.3s, box-shadow 0.3s;
  }

  .emacta__btn--primary {
    background: var(--brand-blue);
    color: #fff;
    box-shadow: 0 4px 20px rgba(55, 93, 251, 0.35);
  }

  .emacta__btn--primary:hover {
    background: var(--brand-blue-hover);
    transform: translateY(-1px);
    box-shadow: 0 6px 28px rgba(55, 93, 251, 0.4);
  }

  .emacta__btn--secondary {
    background: rgba(255, 255, 255, 0.08);
    color: #fff;
    border: 1px solid rgba(255, 255, 255, 0.12);
  }

  .emacta__btn--secondary:hover {
    background: rgba(255, 255, 255, 0.15);
  }

  .emacta__trust {
    display: flex;
    gap: 20px;
    align-items: center;
    margin-top: 8px;
  }

  .emacta__trust-item {
    position: relative;
    padding-left: 14px;
    font-family: var(--font-body);
    font-size: 13px;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.45);
  }

  .emacta__trust-item::before {
    content: "";
    position: absolute;
    left: 0;
    top: 6px;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.45);
  }

  @media (max-width: 1024px) {
    .emacta__card { padding: 60px 24px; }
    .emacta__title { font-size: 36px; }
    .emacta__desc { font-size: 16px; }
  }

  @media (max-width: 767px) {
    .emacta { padding: 40px 16px 60px; }
    .emacta__card { padding: 48px 20px; border-radius: 20px; }
    .emacta__title { font-size: 28px; line-height: 1.2em; }
    .emacta__desc { font-size: 15px; }
    .emacta__proof-row { gap: 8px; }
    .emacta__buttons { flex-direction: column; width: 100%; }
    .emacta__btn { width: 100%; justify-content: center; }
    .emacta__trust { flex-direction: column; gap: 8px; }
  }
</style>
