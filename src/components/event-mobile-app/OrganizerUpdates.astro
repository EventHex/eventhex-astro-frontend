<section class="emau-section">
  <div class="emau-split">
    <div class="emau-copy animate-on-scroll" data-animation="fadeInLeft">
      <span class="emau-kicker">Organizer updates</span>
      <h2>Keep important event information inside the <span>attendee experience</span></h2>
      <p>Publish event notifications through the configured mobile channel so attendees can find updates alongside the programme and other event content.</p>
    </div>
    <div class="emau-product-shot">
      <img src="/images/live-polling-qa/app/mobile-app-updates.png" width="1263" height="697" loading="lazy" decoding="async" alt="EventHex event-app updates workspace for publishing attendee announcements" />
    </div>
  </div>
</section>

<style>
  .emau-section{padding:88px 24px;background:transparent}
  .emau-split{width:min(1120px,100%);margin:auto;display:grid;grid-template-columns:1.05fr .95fr;gap:40px;align-items:center}
  .emau-copy{max-width:560px}
  .emau-kicker{display:block;margin-bottom:14px;color:var(--brand-blue);font:700 12px/1.4 var(--font-body);letter-spacing:.12em;text-transform:uppercase}
  .emau-copy h2{margin:0;font-family:var(--font-heading);font-size:clamp(40px,4.1vw,52px);font-weight:500;line-height:1.115;letter-spacing:-2.1px;background:linear-gradient(98deg,var(--heading-dark) 43.06%,#8f8f8f 91.81%);-webkit-background-clip:text;background-clip:text;color:transparent}
  .emau-copy h2 span{color:inherit}
  .emau-copy>p{font:400 17px/1.65 var(--font-body);color:#667085}
  .emau-product-shot{position:relative;overflow:hidden;padding:14px;border:1px solid #dfe5ef;border-radius:24px;background:linear-gradient(145deg,#f1f5ff,#fff);box-shadow:0 24px 60px rgba(31,52,128,.13)}
  .emau-product-shot:after{content:'';position:absolute;inset:auto 8% -24px;height:48px;border-radius:50%;background:rgba(55,93,251,.2);filter:blur(22px);pointer-events:none}
  .emau-product-shot img{display:block;width:100%;height:auto;border:1px solid #e7eaf0;border-radius:14px;background:#fff;box-shadow:0 18px 42px rgba(31,52,128,.12);animation:emauShotFloat 5.8s ease-in-out infinite;transition:transform .45s ease,box-shadow .45s ease}
  .emau-product-shot:hover img{transform:translateY(-6px) scale(1.015);box-shadow:0 26px 54px rgba(31,52,128,.18);animation-play-state:paused}
  @keyframes emauShotFloat{0%,100%{transform:translateY(4px)}50%{transform:translateY(-6px)}}
  @media(max-width:900px){.emau-split{grid-template-columns:1fr}.emau-copy{max-width:none}}
  @media(max-width:767px){.emau-section{padding:58px 20px}.emau-copy h2{font-size:38px;line-height:1.2;letter-spacing:-1.2px}}
  @media(prefers-reduced-motion:reduce){.emau-section *{animation:none!important;transition:none!important}}
</style>
