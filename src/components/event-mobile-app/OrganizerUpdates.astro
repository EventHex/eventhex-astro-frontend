<section class="emau-section">
  <div class="emau-split">
    <div class="emau-copy animate-on-scroll" data-animation="fadeInLeft">
      <span class="emau-kicker">Organizer updates</span>
      <h2>Keep important event information inside the <span>attendee experience</span></h2>
      <p>Publish event notifications through the configured mobile channel so attendees can find updates alongside the programme and other event content.</p>
    </div>
    <div class="emau-product-shot">
      <img src="/images/live-polling-qa/app/mobile-app-updates.png" width="1263" height="697" loading="lazy" decoding="async" alt="EventHex event-app updates workspace for publishing attendee announcements" />
    </div>
  </div>
</section>

<style>
  .emau-section{padding:64px 24px 112px;background:transparent;color:#fff}
  .emau-split{width:min(1120px,100%);margin:auto;display:grid;grid-template-columns:1.05fr .95fr;gap:40px;align-items:center}
  .emau-copy{max-width:560px}
  .emau-kicker{display:block;margin-bottom:14px;color:rgba(255,255,255,.56);font:700 12px/1.4 var(--font-body);letter-spacing:.12em;text-transform:uppercase}
  .emau-copy h2{margin:0;font-family:var(--font-heading);font-size:clamp(40px,4.1vw,52px);font-weight:500;line-height:1.02;letter-spacing:-2.1px;background:linear-gradient(100deg,#fff 0%,#f2f0ff 35%,#c9c2ff 68%,#8d81ff 100%);-webkit-background-clip:text;background-clip:text;color:transparent}
  .emau-copy h2 span{color:inherit}
  .emau-copy>p{margin:24px 0 0;font:400 17px/1.65 var(--font-body);color:rgba(255,255,255,.6)}
  .emau-product-shot{position:relative;overflow:hidden;padding:14px;border:1px solid #45445b;border-radius:24px;background:linear-gradient(145deg,#171824,#0d0e15);box-shadow:0 24px 60px rgba(0,0,0,.32)}
  .emau-product-shot:after{content:'';position:absolute;inset:auto 8% -24px;height:48px;border-radius:50%;background:rgba(124,108,255,.32);filter:blur(22px);pointer-events:none}
  .emau-product-shot img{display:block;width:100%;height:auto;border:1px solid #343441;border-radius:14px;background:#fff;box-shadow:0 18px 42px rgba(0,0,0,.28);animation:emauShotFloat 5.8s ease-in-out infinite;transition:transform .45s ease,box-shadow .45s ease}
  .emau-product-shot:hover img{transform:translateY(-6px) scale(1.015);box-shadow:0 26px 54px rgba(0,0,0,.38);animation-play-state:paused}
  @keyframes emauShotFloat{0%,100%{transform:translateY(4px)}50%{transform:translateY(-6px)}}
  @media(max-width:900px){.emau-split{grid-template-columns:1fr}.emau-copy{max-width:none}}
  @media(max-width:767px){.emau-section{padding:48px 20px 72px}.emau-copy h2{font-size:38px;line-height:1.05;letter-spacing:-1.2px}}
  @media(prefers-reduced-motion:reduce){.emau-section *{animation:none!important;transition:none!important}}
</style>
