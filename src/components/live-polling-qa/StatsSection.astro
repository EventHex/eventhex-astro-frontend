---
const stats = [
  { value: "500K+", label: "Poll Responses Collected" },
  { value: "85%", label: "Avg. Participation Rate" },
  { value: "< 2s", label: "Response to Screen" },
  { value: "300+", label: "Events Powered" },
];
---

<section class="lpqs-dark">
  <div class="lpqs-engagement">
    <div class="lpqs-engagement-text">
      <h2 class="lpqs-heading animate-on-scroll" data-animation="fadeInDown">
        <span class="lpqs-white">Engage audiences with </span>
        <span class="lpqs-purple">live polls </span>
        <span class="lpqs-white">and </span>
        <span class="lpqs-cyan">moderated Q&A</span>
      </h2>
    </div>
  </div>

  <div class="lpqs-stats">
    {stats.map((stat) => (
      <div class="lpqs-stat">
        <span class="lpqs-stat-value">{stat.value}</span>
        <span class="lpqs-stat-label">{stat.label}</span>
      </div>
    ))}
  </div>
</section>

<style>
  .lpqs-dark {
    display: flex;
    flex-direction: column;
    width: 100%;
    padding: 110px 0;
    background-color: #000;
    background-image:
      radial-gradient(at 57% 33%, hsl(219 0% 0%) 0px, transparent 50%),
      radial-gradient(at 46% 60%, hsl(219 100% 15%) 0px, transparent 50%),
      radial-gradient(at 60% 56%, hsl(327 100% 11%) 0px, transparent 50%),
      radial-gradient(at 49% 0%, hsl(307 100% 11%) 0px, transparent 50%),
      radial-gradient(at 96% 0%, hsl(195 97% 13%) 0px, transparent 50%),
      radial-gradient(at 96% 97%, hsl(206 100% 24%) 0px, transparent 50%),
      radial-gradient(at 8% 82%, hsl(293 96% 10%) 0px, transparent 50%),
      radial-gradient(at 0% 0%, hsl(196 100% 21%) 0px, transparent 50%),
      url('https://cms.eventhex.ai/wp-content/uploads/2024/12/bg-1.webp');
    background-position: center center;
    background-repeat: no-repeat;
    background-size: cover;
  }

  .lpqs-engagement {
    width: 100%;
    padding: 0 0 60px;
    display: flex;
    justify-content: center;
  }

  .lpqs-engagement-text {
    width: 65%;
  }

  .lpqs-heading {
    font-family: var(--font-heading);
    font-size: 48px;
    font-weight: 400;
    line-height: 52px;
    letter-spacing: -1px;
    color: #fff;
    text-align: center;
    margin: 0;
  }

  .lpqs-white { color: #fff; }
  .lpqs-purple { color: #AFA3FF; }
  .lpqs-cyan { color: #ABFFFF; }

  .lpqs-stats {
    display: flex;
    justify-content: center;
    gap: 60px;
    padding: 0 20px;
    max-width: 1100px;
    margin: 0 auto;
    width: 100%;
  }

  .lpqs-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
  }

  .lpqs-stat-value {
    font-family: var(--font-heading);
    font-size: 40px;
    font-weight: 700;
    line-height: 48px;
    letter-spacing: -1px;
    color: #fff;
  }

  .lpqs-stat-label {
    font-family: var(--font-body);
    font-size: 15px;
    font-weight: 400;
    line-height: 20px;
    color: rgba(255, 255, 255, 0.6);
  }

  @media (max-width: 1024px) {
    .lpqs-dark { padding: 80px 0; }
    .lpqs-engagement { padding: 0 20px 40px; }
    .lpqs-engagement-text { width: 85%; }
    .lpqs-heading { font-size: 40px; line-height: 1.1em; }
    .lpqs-stats { gap: 40px; }
    .lpqs-stat-value { font-size: 32px; }
    .lpqs-stat-label { font-size: 14px; }
  }

  @media (max-width: 767px) {
    .lpqs-dark { padding: 64px 0; }
    .lpqs-engagement { padding: 0 15px 30px; }
    .lpqs-engagement-text { width: 100%; }
    .lpqs-heading { font-size: 25px; line-height: 1.2em; letter-spacing: 0; }
    .lpqs-stats {
      flex-wrap: wrap;
      gap: 24px;
      justify-content: center;
    }
    .lpqs-stat { width: 40%; }
    .lpqs-stat-value { font-size: 28px; }
    .lpqs-stat-label { font-size: 13px; }
  }
</style>
