---
---

<section class="apgcta">
  <div class="apgcta__card">
    <div class="apgcta__glow apgcta__glow--1"></div>
    <div class="apgcta__glow apgcta__glow--2"></div>
    <div class="apgcta__content">
      <span class="apgcta__eyebrow animate-on-scroll" data-animation="fadeIn">Launch InstaSnap Faster</span>
      <h2 class="apgcta__title animate-on-scroll" data-animation="fadeInUp">Ready to Deliver Matched Event Photos in Seconds?</h2>
      <p class="apgcta__desc animate-on-scroll" data-animation="fadeInUp">Join organizers using EventHex to turn event photography into a faster attendee experience and a stronger sponsor touchpoint.</p>
      <div class="apgcta__proof-row animate-on-scroll" data-animation="fadeInUp">
        <span class="apgcta__proof-pill">AI selfie matching</span>
        <span class="apgcta__proof-pill">Branded gallery delivery</span>
        <span class="apgcta__proof-pill">Free onboarding support</span>
      </div>
      <div class="apgcta__buttons">
        <a href="/book-a-demo/" class="apgcta__btn apgcta__btn--primary">
          Book a Free Demo
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M6 3l5 5-5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
        </a>
        <a href="/contact-en/" class="apgcta__btn apgcta__btn--secondary">Contact Sales</a>
      </div>
      <div class="apgcta__trust">
        <span class="apgcta__trust-item">No credit card required</span>
        <span class="apgcta__trust-item">Free onboarding support</span>
        <span class="apgcta__trust-item">Cancel anytime</span>
      </div>
    </div>
  </div>
</section>

<style is:global>
  .apgcta { max-width: 1200px; margin: 0 auto; padding: 60px 20px 80px; }

  .apgcta__card {
    position: relative;
    border-radius: 24px;
    padding: 80px 40px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    overflow: hidden;
    background: #0F1629;
  }

  .apgcta__glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(80px);
    pointer-events: none;
  }

  .apgcta__glow--1 {
    width: 400px;
    height: 400px;
    background: rgba(55, 93, 251, 0.2);
    top: -100px;
    left: -50px;
  }

  .apgcta__glow--2 {
    width: 300px;
    height: 300px;
    background: rgba(159, 129, 255, 0.15);
    bottom: -80px;
    right: -30px;
  }

  .apgcta__content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 20px;
    max-width: 600px;
  }

  .apgcta__eyebrow {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 8px 14px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.08);
    border: 1px solid rgba(255, 255, 255, 0.12);
    font-family: var(--font-body);
    font-size: 13px;
    font-weight: 600;
    line-height: 18px;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.72);
  }

  .apgcta__title {
    font-family: var(--font-heading);
    font-size: 48px;
    font-weight: 600;
    line-height: 1.15;
    letter-spacing: -1px;
    color: #fff;
    margin: 0;
  }

  .apgcta__desc {
    font-family: var(--font-body);
    font-size: 18px;
    font-weight: 400;
    line-height: 28px;
    letter-spacing: -0.3px;
    color: rgba(255, 255, 255, 0.65);
    margin: 0;
  }

  .apgcta__proof-row {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 10px;
  }

  .apgcta__proof-pill {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 9px 12px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.08);
    font-family: var(--font-body);
    font-size: 13px;
    font-weight: 500;
    line-height: 18px;
    color: rgba(255, 255, 255, 0.74);
  }

  .apgcta__buttons { display: flex; gap: 12px; justify-content: center; }

  .apgcta__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 6px;
    font-family: var(--font-body);
    font-size: 15px;
    font-weight: 600;
    line-height: 24px;
    letter-spacing: -0.3px;
    padding: 12px 28px;
    border-radius: 10px;
    text-decoration: none;
    transition: background 0.3s, transform 0.3s, box-shadow 0.3s;
  }

  .apgcta__btn--primary {
    background: var(--brand-blue);
    color: #fff;
    box-shadow: 0 4px 20px rgba(55, 93, 251, 0.35);
  }

  .apgcta__btn--primary:hover {
    background: var(--brand-blue-hover);
    transform: translateY(-1px);
    box-shadow: 0 6px 28px rgba(55, 93, 251, 0.4);
  }

  .apgcta__btn--secondary {
    background: rgba(255, 255, 255, 0.08);
    color: #fff;
    border: 1px solid rgba(255, 255, 255, 0.12);
  }

  .apgcta__btn--secondary:hover {
    background: rgba(255, 255, 255, 0.15);
  }

  .apgcta__trust {
    display: flex;
    gap: 20px;
    align-items: center;
    margin-top: 8px;
  }

  .apgcta__trust-item {
    position: relative;
    padding-left: 14px;
    font-family: var(--font-body);
    font-size: 13px;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.45);
  }

  .apgcta__trust-item::before {
    content: "";
    position: absolute;
    left: 0;
    top: 6px;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.45);
  }

  @media (max-width: 1024px) {
    .apgcta__card { padding: 60px 24px; }
    .apgcta__title { font-size: 36px; }
    .apgcta__desc { font-size: 16px; }
  }

  @media (max-width: 767px) {
    .apgcta { padding: 40px 16px 60px; }
    .apgcta__card { padding: 48px 20px; border-radius: 20px; }
    .apgcta__title { font-size: 28px; line-height: 1.2em; }
    .apgcta__desc { font-size: 15px; }
    .apgcta__proof-row { gap: 8px; }
    .apgcta__buttons { flex-direction: column; width: 100%; }
    .apgcta__btn { width: 100%; justify-content: center; }
    .apgcta__trust { flex-direction: column; gap: 8px; }
  }
</style>