---
const heroPoints = [
  "Selfie-to-photo matching in seconds",
  "Bulk uploads for photographers",
  "Sponsor-ready branded galleries",
];

const trustItems = [
  "50K+ photos processed",
  "Trusted by 400+ events",
];

const metrics = [
  { value: "95%+", label: "match accuracy" },
  { value: "3s", label: "average match time" },
  { value: "50K+", label: "photos processed" },
];
---

<section class="apgh-hero">
  <div class="apgh-hero__shell">
    <div class="apgh-hero__grid">
      <div class="apgh-hero__content">
        <div class="apgh-hero__badge animate-on-scroll" data-animation="fadeInDown">
          <span>AI Photo Gallery</span>
        </div>

        <h1 class="apgh-hero__title animate-on-scroll" data-animation="fadeInUp">
          Run Event Photo Ops Without
          <span>Manual Sorting Chaos</span>
        </h1>

        <p class="apgh-hero__desc animate-on-scroll" data-animation="fadeInUp" style="--anim-duration: 800ms">
          Upload event photos, let attendees take a selfie, and deliver their matched results in seconds.
          EventHex's AI photo gallery combines face matching, gallery delivery, sponsor branding, and privacy controls in one workflow.
        </p>

        <div class="apgh-hero__points animate-on-scroll" data-animation="fadeInUp" style="--anim-duration: 950ms">
          {heroPoints.map((point) => <span class="apgh-hero__point">{point}</span>)}
        </div>

        <div class="apgh-hero__cta animate-on-scroll" data-animation="fadeInUp" style="--anim-duration: 1100ms">
          <a href="/book-a-demo/" class="apgh-hero__btn apgh-hero__btn--primary">Get a Free Demo</a>
          <a href="#gallery-bento" class="apgh-hero__btn apgh-hero__btn--secondary">Explore Features</a>
        </div>

        <div class="apgh-hero__trust animate-on-scroll" data-animation="fadeIn" style="--anim-duration: 1250ms">
          {trustItems.map((item) => <span class="apgh-hero__trust-item">{item}</span>)}
        </div>
      </div>

      <div class="apgh-hero__visual animate-on-scroll" data-animation="fadeInRight">
        <img
          src="/feature-images/ai-photo-gallery/hero-visual.svg"
          alt="EventHex AI event photo gallery with face matching"
          class="apgh-hero__visual-svg"
          width="560"
          height="460"
          loading="eager"
        />
      </div>
    </div>
  </div>
</section>

<style is:global>
  .apgh-hero {
    position: relative;
    overflow: hidden;
    margin-top: -90px;
    padding: 0 0 220px;
    background:
      radial-gradient(circle at 16% 22%, rgba(175, 163, 255, 0.16), transparent 24%),
      radial-gradient(circle at 82% 20%, rgba(171, 255, 255, 0.16), transparent 26%),
      url('https://cms.eventhex.ai/wp-content/uploads/2024/12/Untitled-design-11-1024x576.webp') center center / cover no-repeat;
  }

  .apgh-hero__shell {
    position: relative;
    z-index: 1;
    width: 100%;
    padding: 160px 0 0;
    background: url('https://cms.eventhex.ai/wp-content/uploads/2024/12/Frame-48096154-1.webp') center center / cover no-repeat;
  }

  .apgh-hero__grid {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 20px 60px;
    display: grid;
    grid-template-columns: minmax(0, 1.02fr) minmax(0, 1fr);
    gap: 48px;
    align-items: center;
  }

  .apgh-hero__content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 22px;
  }

  .apgh-hero__badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-family: var(--font-body);
    font-size: 18px;
    font-weight: 400;
    line-height: 24px;
    letter-spacing: -0.48px;
    color: var(--subheading-gray);
  }

  .apgh-hero__title {
    margin: 0;
    font-family: var(--font-heading);
    font-size: 52px;
    font-weight: 600;
    line-height: 60px;
    letter-spacing: -1px;
    color: var(--heading-dark);
  }

  .apgh-hero__title span {
    display: block;
    color: var(--brand-blue);
  }

  .apgh-hero__desc {
    margin: 0;
    max-width: 580px;
    font-family: var(--font-body);
    font-size: 18px;
    font-weight: 400;
    line-height: 28px;
    letter-spacing: -0.48px;
    color: var(--subheading-gray);
  }

  .apgh-hero__points {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
  }

  .apgh-hero__point {
    display: inline-flex;
    align-items: center;
    padding: 9px 14px;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.9);
    border: 1px solid #e5ebff;
    font-family: var(--font-body);
    font-size: 14px;
    font-weight: 500;
    line-height: 20px;
    color: #475467;
    box-shadow: 0 8px 24px rgba(34, 39, 47, 0.05);
  }

  .apgh-hero__cta {
    display: flex;
    gap: 14px;
    padding-top: 6px;
  }

  .apgh-hero__btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 48px;
    padding: 12px 32px;
    border-radius: 10px;
    text-decoration: none;
    font-family: var(--font-body);
    font-size: 15px;
    font-weight: 600;
    line-height: 24px;
    letter-spacing: -0.48px;
    transition: transform 0.3s, box-shadow 0.3s, background 0.3s, border-color 0.3s;
  }

  .apgh-hero__btn--primary {
    color: #fff;
    background: var(--brand-blue);
    box-shadow: 0 10px 24px rgba(55, 93, 251, 0.28);
  }

  .apgh-hero__btn--primary:hover {
    transform: translateY(-2px);
    background: var(--brand-blue-hover);
    box-shadow: 0 14px 30px rgba(55, 93, 251, 0.34);
  }

  .apgh-hero__btn--secondary {
    color: var(--heading-dark);
    background: rgba(255, 255, 255, 0.9);
    border: 1px solid #dce4ff;
  }

  .apgh-hero__btn--secondary:hover {
    transform: translateY(-2px);
    border-color: #c5d2ff;
    box-shadow: 0 10px 24px rgba(34, 39, 47, 0.06);
  }

  .apgh-hero__trust {
    display: flex;
    flex-wrap: wrap;
    gap: 20px;
  }

  .apgh-hero__trust-item {
    position: relative;
    padding-left: 16px;
    font-family: var(--font-body);
    font-size: 14px;
    font-weight: 500;
    line-height: 20px;
    color: #667085;
  }

  .apgh-hero__trust-item::before {
    content: "";
    position: absolute;
    left: 0;
    top: 8px;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #10b981;
  }

  .apgh-hero__visual {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .apgh-hero__visual-svg {
    width: 100%;
    max-width: 560px;
    height: auto;
    object-fit: contain;
    animation: apghHeroFloat 5s ease-in-out infinite;
  }

  @keyframes apghHeroFloat {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-14px); }
  }

  @media (max-width: 1120px) {
    .apgh-hero { padding: 0 20px 110px; }
    .apgh-hero__shell { padding: 136px 0 0; }
    .apgh-hero__grid { gap: 28px; padding: 0 0 40px; }
    .apgh-hero__desc { max-width: 520px; }
    .apgh-hero__title { font-size: 46px; line-height: 1.1em; }
  }

  @media (max-width: 960px) {
    .apgh-hero { padding: 0 20px 96px; }
    .apgh-hero__shell { padding: 130px 0 0; }
    .apgh-hero__grid { grid-template-columns: 1fr; gap: 34px; padding: 0 0 40px; }
    .apgh-hero__content { align-items: center; text-align: center; }
    .apgh-hero__desc { max-width: 720px; }
    .apgh-hero__trust,
    .apgh-hero__points,
    .apgh-hero__cta { justify-content: center; }
    .apgh-hero__title { font-size: 42px; line-height: 1.1em; }
    .apgh-hero__visual-card { padding: 26px 0 0; }
    .apgh-hero__floating--top { left: 0; }
    .apgh-hero__floating--bottom { right: 0; bottom: 34px; }
  }

  @media (max-width: 767px) {
    .apgh-hero { margin-top: -80px; padding: 0 20px 90px; }
    .apgh-hero__shell { padding: 120px 0 0; }
    .apgh-hero__grid { padding: 0 0 30px; }
    .apgh-hero__title { font-size: 30px; line-height: 1.2em; letter-spacing: 0; }
    .apgh-hero__desc { font-size: 15px; line-height: 1.7; }
    .apgh-hero__badge { font-size: 16px; line-height: 1.1em; }
    .apgh-hero__point { font-size: 13px; padding: 8px 12px; }
    .apgh-hero__cta { width: 100%; flex-direction: column; }
    .apgh-hero__btn { width: 100%; }
    .apgh-hero__trust { gap: 10px; justify-content: center; }
    .apgh-hero__floating { position: static; margin-bottom: 12px; max-width: none; }
    .apgh-hero__visual-card { padding: 18px 0 0; }
    .apgh-hero__metric-row { grid-template-columns: 1fr; }
  }
</style>