<section class="emh-hero">
  <div class="emh-hero__grid">
    <div class="emh-hero__content">
      <span class="emh-hero__badge animate-on-scroll" data-animation="fadeInDown">Event Marketing</span>
      <h1 class="emh-hero__title animate-on-scroll" data-animation="fadeInUp">
        Build Event Campaigns With the Audience Data You Already Have
      </h1>
      <p class="emh-hero__desc animate-on-scroll" data-animation="fadeInUp">
        Build email and WhatsApp campaigns around current registrants, selected ticket groups, saved audiences, people segments, or registrants from previous events. Personalize the message, send now or schedule it, and follow delivery from the same EventHex workspace.
      </p>
      <div class="emh-hero__cta-row animate-on-scroll" data-animation="fadeInUp">
        <a href="/book-a-demo/" class="emh-hero__btn">
          Book a Demo
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M6 3l5 5-5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg>
        </a>
        <a href="#em-channel-choice" class="emh-hero__btn emh-hero__btn--outline">See the campaign workflow</a>
      </div>
      <div class="emh-hero__trust animate-on-scroll" data-animation="fadeIn">
        <span class="emh-hero__trust-item">Email and WhatsApp campaigns</span>
        <span class="emh-hero__trust-item">Registration-aware audiences</span>
      </div>
    </div>

    <div class="emh-hero__visual animate-on-scroll" data-animation="fadeInRight">
      <div class="emh-hero__visual-card">
        <div class="emh-hero__visual-inner">
          <div class="emh-hero__collage">
            <figure class="emh-hero__shot emh-hero__shot--campaign">
              <img
                src="/feature-images/solutions/event-marketing-email-campaign-hero.jpg"
                alt="EventHex Email Campaigns workspace showing a completed campaign with send and open metrics"
                width="900"
                height="340"
                loading="eager"
                decoding="async"
                fetchpriority="high"
              />
            </figure>
            <figure class="emh-hero__shot emh-hero__shot--audience">
              <img
                src="/feature-images/solutions/event-marketing-audience-panel.jpg"
                alt="EventHex campaign audience builder with event, segment, ticket, and saved-audience options"
                width="770"
                height="754"
                loading="eager"
                decoding="async"
              />
            </figure>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<section class="emh-trust-strip" aria-label="Event marketing workflow summary">
  <div class="emh-trust-strip__inner">
    <span class="emh-trust-strip__stat"><b>Audience</b> Choose who should receive it</span>
    <span class="emh-trust-strip__stat"><b>Channel</b> Email or WhatsApp</span>
    <span class="emh-trust-strip__stat"><b>Message</b> Create and personalize</span>
    <span class="emh-trust-strip__stat"><b>Schedule</b> Send now or later</span>
    <span class="emh-trust-strip__stat"><b>Monitor</b> Follow every status</span>
  </div>
</section>

<style>
  /* Exact Event Registration hero and trust-strip structure, with marketing content. */
  .emh-hero {
    position: relative;
    overflow: hidden;
    margin-top: -90px;
    padding: 140px 24px 64px;
    background:
      radial-gradient(ellipse 700px 500px at -5% -10%, #375dfb24, #375dfb0f 40%, #375dfb00 75%),
      radial-gradient(ellipse 800px 600px at 100% 105%, #ff74b83d, #ff74b81a 45%, #ff74b800 78%),
      #fff;
  }
  .emh-hero::after {
    content: "";
    position: absolute;
    inset: 0 0 0 auto;
    width: 220px;
    max-width: 30vw;
    background: linear-gradient(to right, #fff0, #ffffff73 70%, #ffffffbf);
    pointer-events: none;
    z-index: 2;
  }
  .emh-hero__grid {
    max-width: 1080px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: minmax(260px, 542px) minmax(0, 490px);
    gap: 48px;
    align-items: center;
  }
  .emh-hero__content { display: flex; flex-direction: column; align-items: flex-start; gap: 20px; }
  .emh-hero__badge { font: 500 16px/24px var(--font-body); color: var(--brand-blue); }
  .emh-hero__title {
    width: 100%;
    margin: 0;
    font: 500 60px/66px var(--font-heading);
    letter-spacing: -2.1px;
    background: linear-gradient(98deg, var(--heading-dark) 43.06%, #8f8f8f 91.81%);
    background-clip: text;
    -webkit-background-clip: text;
    color: transparent;
  }
  .emh-hero__desc { width: 100%; margin: 0; color: var(--subheading-gray); font: 400 18px/24px var(--font-body); letter-spacing: -.25px; }
  .emh-hero__cta-row { display: flex; align-items: center; flex-wrap: wrap; gap: 16px; padding-top: 8px; }
  .emh-hero__btn { display: inline-flex; align-items: center; gap: 6px; padding: 12px 20px; border: 1px solid transparent; border-radius: 12px; color: #fff; background: var(--heading-dark); font: 650 15px/22px var(--font-body); letter-spacing: -.3px; text-decoration: none; transition: transform .3s, background .3s; }
  .emh-hero__btn:hover { transform: translateY(-2px); background: #000; }
  .emh-hero__btn--outline { border-color: rgba(16,24,40,.22); color: var(--heading-dark); background: transparent; }
  .emh-hero__btn--outline:hover { border-color: var(--heading-dark); background: transparent; }
  .emh-hero__trust { display: flex; flex-wrap: wrap; gap: 18px; padding-top: 4px; }
  .emh-hero__trust-item { position: relative; padding-left: 16px; color: var(--heading-dark); font: 400 16px/1.4 var(--font-body); }
  .emh-hero__trust-item::before { content: ""; position: absolute; left: 0; top: 9px; width: 6px; height: 6px; border-radius: 50%; background: #10b981; }
  .emh-hero__visual { position: relative; margin-right: calc(-1 * (max(0px, (100vw - 1128px) / 2) + 24px)); }
  .emh-hero__visual-card { position: relative; padding: 14px; border: 1px solid rgba(255,255,255,.62); border-right: 0; border-radius: 22px 0 0 22px; background: linear-gradient(180deg,#ffffff9e,#ffffff47),#ffffff57; backdrop-filter: blur(16px); }
  .emh-hero__visual-inner { position: relative; padding: 24px; border-radius: 16px 0 0 16px; overflow: hidden; background: linear-gradient(135deg,#d9ecfa,#eef6fc 45%,#fff); }
  .emh-hero__collage { position: relative; min-height: 380px; isolation: isolate; }
  .emh-hero__shot { position: absolute; margin: 0; overflow: hidden; border: 1px solid rgba(29,41,57,.1); border-radius: 12px; background: #fff; box-shadow: 0 20px 50px rgba(30,52,92,.16); }
  .emh-hero__shot img { display: block; width: 100%; height: auto; }
  .emh-hero__shot--campaign { top: 28px; left: 0; z-index: 1; width: 92%; }
  .emh-hero__shot--audience { right: 0; bottom: 14px; z-index: 2; width: 55%; }
  @media (min-width: 901px) {
    .emh-hero__shot--audience { right: -18px; }
  }
  .emh-trust-strip { padding: 1.5rem; border-top: 1px solid var(--surface-card-border); }
  .emh-trust-strip__inner { max-width: 1280px; margin: 0 auto; display: flex; align-items: center; justify-content: center; flex-wrap: wrap; gap: 2.25rem; }
  .emh-trust-strip__stat { color: var(--subheading-gray); font: 400 .9375rem/1.4 var(--font-body); white-space: nowrap; }
  .emh-trust-strip__stat b { color: var(--heading-dark); font-weight: 700; }
  @media (max-width: 900px) {
    .emh-hero__grid { grid-template-columns: 1fr; }
    .emh-hero__visual { margin-right: -24px; }
    .emh-hero__title { font-size: 52px; line-height: 1.05; }
  }
  @media (max-width: 767px) {
    .emh-hero { padding: 128px 18px 48px; }
    .emh-hero__content { align-items: center; text-align: center; }
    .emh-hero__title { font-size: 43px; line-height: 1.03; letter-spacing: -1.7px; }
    .emh-hero__desc { font-size: 16px; line-height: 24px; }
    .emh-hero__cta-row { width: 100%; }
    .emh-hero__btn { justify-content: center; width: 100%; }
    .emh-hero__trust { justify-content: center; }
    .emh-hero__visual { width: calc(100% + 18px); margin-right: -18px; }
    .emh-hero__visual-inner { padding: 16px; }
    .emh-hero__collage { min-height: 320px; }
    .emh-hero__shot--campaign { top: 18px; width: 94%; }
    .emh-hero__shot--audience { bottom: 10px; width: 57%; }
    .emh-trust-strip__inner { align-items: flex-start; flex-direction: column; gap: 12px; }
  }
</style>
