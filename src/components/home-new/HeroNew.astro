---
// Source: Elementor page 902
// Container c6735b9 (depth 0): hero outer — bg Untitled-design-11, margin-top -90px, padding-bottom 220px
//   Container 2e95e2d (depth 1): inner — bg Frame-48096154-1, padding-top 160px, flex column, align center
//     Container 8228008 (depth 2): justify center, align center, padding-bottom 60px
//       Container e547529 (depth 3): boxed 670px, flex column, gap 32px, align center
//         Container 02bf690 (depth 4): flex column, gap 16px, align center
//           Widget ac17d09: heading "Simplify Events | Amplify Results" — Inter 18px/24px/400, #475467, fadeIn slow 300ms
//           Widget 67a1a19: heading H1 — Manrope 52px/60px/600, -1px tracking, max-width 748px
//         Container 906c86b (depth 4): row, justify center
//           Widget 8de768f: button "Request a demo" — Inter 15px/600, #375DFB, radius 10px, hover grow
---

<section class="hero-outer">
  <div class="hero-inner">
    <div class="hero-center">
      <div class="hero-content">
        <div class="hero-text">
          <!-- Subtitle: ac17d09 -->
          <p
            class="hero-subtitle animate-on-scroll"
            data-animation="fadeIn"
            data-delay="300"
            style="--anim-duration: 1000ms"
          >
            Simplify Events | Amplify Results
          </p>
          <!-- H1: 67a1a19 -->
          <h1 class="hero-title">
            The AI-Powered Event Management Platform
            for Every Event
          </h1>
        </div>
        <!-- Button container: 906c86b -->
        <div class="hero-cta">
          <a href="/book-a-demo/" class="hero-button">Request a demo</a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* c6735b9 — outer section */
  .hero-outer {
    position: relative;
    background: url('https://cms.eventhex.ai/wp-content/uploads/2024/12/Untitled-design-11-1024x576.webp') center center / cover no-repeat;
    margin-top: -90px;
    padding: 0 0 220px 0;
  }

  /* 2e95e2d — inner with Frame bg */
  .hero-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    background: url('https://cms.eventhex.ai/wp-content/uploads/2024/12/Frame-48096154-1.webp') center center / cover no-repeat;
    padding: 160px 0 0 0;
  }

  /* 8228008 — centering wrapper */
  .hero-center {
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 0 0 60px 0;
  }

  /* e547529 — content box, boxed 670px, gap 32px */
  .hero-content {
    max-width: 670px;
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 32px;
    margin: 0;
  }

  /* 02bf690 — text group, gap 16px */
  .hero-text {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 16px;
    width: 100%;
  }

  /* ac17d09 — subtitle */
  .hero-subtitle {
    font-family: var(--font-body);
    font-size: 18px;
    line-height: 24px;
    font-weight: 400;
    letter-spacing: -0.48px;
    color: var(--subheading-gray);
    text-align: center;
    margin: 0;
  }

  /* 67a1a19 — H1 */
  .hero-title {
    font-family: var(--font-heading);
    font-size: 52px;
    line-height: 60px;
    font-weight: 600;
    letter-spacing: -1px;
    color: var(--heading-dark);
    text-align: center;
    max-width: 748px;
    margin: 0;
    align-self: center;
    white-space: pre-line;
  }

  /* 906c86b — cta container */
  .hero-cta {
    display: flex;
    justify-content: center;
    width: 100%;
  }

  /* 8de768f — button */
  .hero-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-body);
    font-size: 15px;
    line-height: 24px;
    font-weight: 600;
    letter-spacing: -0.48px;
    color: #fff;
    background: var(--brand-blue);
    border-radius: 10px;
    padding: 12px 32px;
    text-decoration: none;
    transition: transform 0.3s, background 0.3s;
    align-self: center;
  }
  .hero-button:hover {
    background: var(--brand-blue-hover);
    transform: scale(1.05);
  }

  /* Tablet (1024px) */
  @media (max-width: 1024px) {
    .hero-outer { padding: 0 20px 90px 20px; }
    .hero-inner { padding: 130px 0 0 0; }
    .hero-content { max-width: 500px; }
    .hero-subtitle { font-size: 16px; }
    .hero-title { font-size: 40px; line-height: 1.1em; }
  }

  /* Mobile (767px) */
  @media (max-width: 767px) {
    .hero-outer { padding: 0 20px 120px 20px; }
    .hero-inner { padding: 120px 0 0 0; }
    .hero-subtitle { font-size: 16px; line-height: 1.1em; width: 70%; }
    .hero-title { font-size: 30px; line-height: 1.2em; letter-spacing: 0; }
    .hero-button { font-size: 14px; padding: 8px 10px; }
  }
</style>
