---
const stats = [
  { value: "50+", label: "Report Templates" },
  { value: "1M+", label: "Data Points Processed" },
  { value: "< 5s", label: "Dashboard Load Time" },
  { value: "4.8/5", label: "Organizer Satisfaction" },
];
---

<section class="eas-dark">
  <div class="eas-engagement">
    <div class="eas-engagement-text">
      <h2 class="eas-heading animate-on-scroll" data-animation="fadeInDown">
        <span class="eas-white">Analyze events with </span>
        <span class="eas-purple">real-time dashboards </span>
        <span class="eas-white">and </span>
        <span class="eas-cyan">AI-powered insights</span>
      </h2>
    </div>
  </div>

  <div class="eas-stats">
    {stats.map((stat) => (
      <div class="eas-stat">
        <span class="eas-stat-value">{stat.value}</span>
        <span class="eas-stat-label">{stat.label}</span>
      </div>
    ))}
  </div>
</section>

<style>
  .eas-dark {
    display: flex;
    flex-direction: column;
    width: 100%;
    padding: 110px 0;
    background-color: #000;
    background-image:
      radial-gradient(at 57% 33%, hsl(219 0% 0%) 0px, transparent 50%),
      radial-gradient(at 46% 60%, hsl(219 100% 15%) 0px, transparent 50%),
      radial-gradient(at 60% 56%, hsl(327 100% 11%) 0px, transparent 50%),
      radial-gradient(at 49% 0%, hsl(307 100% 11%) 0px, transparent 50%),
      radial-gradient(at 96% 0%, hsl(195 97% 13%) 0px, transparent 50%),
      radial-gradient(at 96% 97%, hsl(206 100% 24%) 0px, transparent 50%),
      radial-gradient(at 8% 82%, hsl(293 96% 10%) 0px, transparent 50%),
      radial-gradient(at 0% 0%, hsl(196 100% 21%) 0px, transparent 50%),
      url('https://cms.eventhex.ai/wp-content/uploads/2024/12/bg-1.webp');
    background-position: center center;
    background-repeat: no-repeat;
    background-size: cover;
  }

  .eas-engagement {
    width: 100%;
    padding: 0 0 60px;
    display: flex;
    justify-content: center;
  }

  .eas-engagement-text {
    width: 65%;
  }

  .eas-heading {
    font-family: var(--font-heading);
    font-size: 48px;
    font-weight: 400;
    line-height: 52px;
    letter-spacing: -1px;
    color: #fff;
    text-align: center;
    margin: 0;
  }

  .eas-white { color: #fff; }
  .eas-purple { color: #AFA3FF; }
  .eas-cyan { color: #ABFFFF; }

  .eas-stats {
    display: flex;
    justify-content: center;
    gap: 60px;
    padding: 0 20px;
    max-width: 1100px;
    margin: 0 auto;
    width: 100%;
  }

  .eas-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
  }

  .eas-stat-value {
    font-family: var(--font-heading);
    font-size: 40px;
    font-weight: 700;
    line-height: 48px;
    letter-spacing: -1px;
    color: #fff;
  }

  .eas-stat-label {
    font-family: var(--font-body);
    font-size: 15px;
    font-weight: 400;
    line-height: 20px;
    color: rgba(255, 255, 255, 0.6);
  }

  @media (max-width: 1024px) {
    .eas-dark { padding: 80px 0; }
    .eas-engagement { padding: 0 20px 40px; }
    .eas-engagement-text { width: 85%; }
    .eas-heading { font-size: 40px; line-height: 1.1em; }
    .eas-stats { gap: 40px; }
    .eas-stat-value { font-size: 32px; }
    .eas-stat-label { font-size: 14px; }
  }

  @media (max-width: 767px) {
    .eas-dark { padding: 64px 0; }
    .eas-engagement { padding: 0 15px 30px; }
    .eas-engagement-text { width: 100%; }
    .eas-heading { font-size: 25px; line-height: 1.2em; letter-spacing: 0; }
    .eas-stats {
      flex-wrap: wrap;
      gap: 24px;
      justify-content: center;
    }
    .eas-stat { width: 40%; }
    .eas-stat-value { font-size: 28px; }
    .eas-stat-label { font-size: 13px; }
  }
</style>
