---
const stats = [
  { value: "1,000+", label: "Websites Built" },
  { value: "40+", label: "Templates Available" },
  { value: "3x", label: "Faster Than Custom Dev" },
  { value: "99.9%", label: "Uptime Guarantee" },
];
---

<section class="ewbs-dark">
  <div class="ewbs-engagement">
    <div class="ewbs-engagement-text">
      <h2 class="ewbs-heading animate-on-scroll" data-animation="fadeInDown">
        <span class="ewbs-white">Build event websites with </span>
        <span class="ewbs-purple">drag-and-drop </span>
        <span class="ewbs-white">and </span>
        <span class="ewbs-cyan">zero code</span>
      </h2>
    </div>
  </div>

  <div class="ewbs-stats">
    {stats.map((stat) => (
      <div class="ewbs-stat">
        <span class="ewbs-stat-value">{stat.value}</span>
        <span class="ewbs-stat-label">{stat.label}</span>
      </div>
    ))}
  </div>
</section>

<style>
  .ewbs-dark {
    display: flex;
    flex-direction: column;
    width: 100%;
    padding: 110px 0;
    background-color: #000;
    background-image:
      radial-gradient(at 57% 33%, hsl(219 0% 0%) 0px, transparent 50%),
      radial-gradient(at 46% 60%, hsl(219 100% 15%) 0px, transparent 50%),
      radial-gradient(at 60% 56%, hsl(327 100% 11%) 0px, transparent 50%),
      radial-gradient(at 49% 0%, hsl(307 100% 11%) 0px, transparent 50%),
      radial-gradient(at 96% 0%, hsl(195 97% 13%) 0px, transparent 50%),
      radial-gradient(at 96% 97%, hsl(206 100% 24%) 0px, transparent 50%),
      radial-gradient(at 8% 82%, hsl(293 96% 10%) 0px, transparent 50%),
      radial-gradient(at 0% 0%, hsl(196 100% 21%) 0px, transparent 50%),
      url('https://cms.eventhex.ai/wp-content/uploads/2024/12/bg-1.webp');
    background-position: center center;
    background-repeat: no-repeat;
    background-size: cover;
  }

  .ewbs-engagement {
    width: 100%;
    padding: 0 0 60px;
    display: flex;
    justify-content: center;
  }

  .ewbs-engagement-text {
    width: 65%;
  }

  .ewbs-heading {
    font-family: var(--font-heading);
    font-size: 48px;
    font-weight: 400;
    line-height: 52px;
    letter-spacing: -1px;
    color: #fff;
    text-align: center;
    margin: 0;
  }

  .ewbs-white { color: #fff; }
  .ewbs-purple { color: #AFA3FF; }
  .ewbs-cyan { color: #ABFFFF; }

  .ewbs-stats {
    display: flex;
    justify-content: center;
    gap: 60px;
    padding: 0 20px;
    max-width: 1100px;
    margin: 0 auto;
    width: 100%;
  }

  .ewbs-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
  }

  .ewbs-stat-value {
    font-family: var(--font-heading);
    font-size: 40px;
    font-weight: 700;
    line-height: 48px;
    letter-spacing: -1px;
    color: #fff;
  }

  .ewbs-stat-label {
    font-family: var(--font-body);
    font-size: 15px;
    font-weight: 400;
    line-height: 20px;
    color: rgba(255, 255, 255, 0.6);
  }

  @media (max-width: 1024px) {
    .ewbs-dark { padding: 80px 0; }
    .ewbs-engagement { padding: 0 20px 40px; }
    .ewbs-engagement-text { width: 85%; }
    .ewbs-heading { font-size: 40px; line-height: 1.1em; }
    .ewbs-stats { gap: 40px; }
    .ewbs-stat-value { font-size: 32px; }
    .ewbs-stat-label { font-size: 14px; }
  }

  @media (max-width: 767px) {
    .ewbs-dark { padding: 64px 0; }
    .ewbs-engagement { padding: 0 15px 30px; }
    .ewbs-engagement-text { width: 100%; }
    .ewbs-heading { font-size: 25px; line-height: 1.2em; letter-spacing: 0; }
    .ewbs-stats {
      flex-wrap: wrap;
      gap: 24px;
      justify-content: center;
    }
    .ewbs-stat { width: 40%; }
    .ewbs-stat-value { font-size: 28px; }
    .ewbs-stat-label { font-size: 13px; }
  }
</style>
