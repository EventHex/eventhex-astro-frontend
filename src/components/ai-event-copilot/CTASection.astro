<section class="aeccta">
  <div class="aeccta__inner animate-on-scroll" data-animation="fadeInUp">
    <span>Bring one real event question</span>
    <h2>See how EventHex Copilot would handle the next task on your list</h2>
    <p>Show us the workflow your team keeps switching screens to complete. We’ll map the question, data, supported action, and control point in a focused EventHex demo.</p>
    <a href="/book-a-demo/">Book a Demo</a>
  </div>
</section>

<style>
  .aeccta { position: relative; overflow: hidden; padding: clamp(96px, 11vw, 156px) 24px; text-align: center; background: #030407; }
  .aeccta::before { content: ""; position: absolute; inset: 0; background: radial-gradient(55% 70% at 28% 100%, #38bdf842, transparent 72%), radial-gradient(50% 70% at 78% 50%, #a45cff33, transparent 74%); }
  .aeccta::after { content: ""; position: absolute; inset: 0; opacity: .35; background-image: repeating-linear-gradient(0deg, #ffffff12 0 1px, transparent 1px 46px); mask-image: repeating-linear-gradient(90deg, #000 0 10px, transparent 10px 24px); }
  .aeccta__inner { position: relative; z-index: 1; display: flex; flex-direction: column; align-items: center; gap: 22px; max-width: 900px; margin: 0 auto; }
  .aeccta__inner > span { color: #b8c4ff; font: 700 12px/1 var(--font-body); letter-spacing: .11em; text-transform: uppercase; }
  .aeccta h2 { margin: 0; color: #fff; font: 500 var(--aec-section-title-size)/var(--aec-section-title-line) var(--font-heading); letter-spacing: var(--aec-section-title-tracking); }
  .aeccta p { max-width: 720px; margin: 0; color: #c1c8d5; font: 16px/1.65 var(--font-body); }
  .aeccta a { display: inline-flex; align-items: center; justify-content: center; padding: 14px 28px; border-radius: 12px; color: #14151a; background: #fff; font: 650 16px/1.3 var(--font-body); text-decoration: none; transition: transform .3s ease; }
  .aeccta a:hover { transform: translateY(-2px); }
  @media (max-width: 560px) { .aeccta { padding: 72px 16px; }.aeccta__inner { gap: 18px; }.aeccta p { font-size: 15px; }.aeccta a { width: 100%; } }
  @media (prefers-reduced-motion: reduce) { .aeccta a { transition: none; } }
</style>
