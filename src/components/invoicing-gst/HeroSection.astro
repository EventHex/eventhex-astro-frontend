<section class="igh-hero">
  <div class="igh-hero__grid">
    <div class="igh-hero__content">
      <span class="igh-hero__badge animate-on-scroll" data-animation="fadeInDown">Event invoicing and tax</span>
      <h1 class="igh-hero__title animate-on-scroll" data-animation="fadeInUp">Create event-order invoices with the tax detail your market needs</h1>
      <p class="igh-hero__desc animate-on-scroll" data-animation="fadeInUp">Collect buyer billing details, apply your approved GST, VAT, or custom tax lines, and deliver a numbered PDF invoice connected to the original event order.</p>
      <div class="igh-hero__cta-row animate-on-scroll" data-animation="fadeInUp">
        <a href="/book-a-demo/" class="igh-hero__btn">Book a Demo <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true"><path d="m6 3 5 5-5 5" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path></svg></a>
        <a href="#ig-workflow" class="igh-hero__btn igh-hero__btn--outline">See the invoice workflow</a>
      </div>
      <div class="igh-hero__trust animate-on-scroll" data-animation="fadeIn">
        <span class="igh-hero__trust-item">Organizer-defined invoice identity</span>
        <span class="igh-hero__trust-item">Buyer billing and tax fields</span>
        <span class="igh-hero__trust-item">Order-linked PDF delivery</span>
      </div>
    </div>
    <div class="igh-hero__visual animate-on-scroll" data-animation="fadeInRight">
      <div class="igh-hero__visual-card">
        <div class="igh-hero__visual-inner">
          <img src="/feature-images/invoicing-gst/hero-invoice-workflow.svg" alt="EventHex workspace showing buyer details, an organizer-configured VAT line, and a ready PDF invoice linked to an event order" width="1200" height="900" loading="eager" decoding="async" fetchpriority="high" />
        </div>
      </div>
    </div>
  </div>
</section>

<section class="igh-trust-strip" aria-label="Event invoice workflow summary">
  <div class="igh-trust-strip__inner">
    <span class="igh-trust-strip__stat"><b>Configure</b> Identity + numbering</span>
    <span class="igh-trust-strip__stat"><b>Request</b> Buyer chooses invoice</span>
    <span class="igh-trust-strip__stat"><b>Collect</b> Billing fields</span>
    <span class="igh-trust-strip__stat"><b>Calculate</b> Named tax lines</span>
    <span class="igh-trust-strip__stat"><b>Generate</b> Numbered PDF</span>
    <span class="igh-trust-strip__stat"><b>Deliver</b> Order-linked email</span>
  </div>
</section>

<style>
  /* Event Registration split hero and trust-strip pattern. */
  .igh-hero { position:relative; overflow:hidden; margin-top:-90px; padding:140px 24px 64px; background:radial-gradient(ellipse 700px 500px at -5% -10%,#375dfb24,#375dfb0f 40%,#375dfb00 75%),radial-gradient(ellipse 800px 600px at 100% 105%,#ff74b83d,#ff74b81a 45%,#ff74b800 78%),#fff; }
  .igh-hero::after { content:""; position:absolute; inset:0 0 0 auto; z-index:2; width:220px; max-width:30vw; background:linear-gradient(to right,#fff0,#ffffff73 70%,#ffffffbf); pointer-events:none; }
  .igh-hero__grid { max-width:1080px; margin:0 auto; display:grid; grid-template-columns:minmax(260px,542px) minmax(0,490px); gap:48px; align-items:center; }
  .igh-hero__content { display:flex; flex-direction:column; align-items:flex-start; gap:20px; }
  .igh-hero__badge { color:var(--brand-blue); font:500 16px/24px var(--font-body); text-transform:uppercase; }
  .igh-hero__title { width:100%; margin:0; font:500 58px/64px var(--font-heading); letter-spacing:-2.1px; background:linear-gradient(98deg,var(--heading-dark) 43.06%,#8f8f8f 91.81%); background-clip:text; -webkit-background-clip:text; color:transparent; }
  .igh-hero__desc { width:100%; margin:0; color:var(--subheading-gray); font:400 18px/24px var(--font-body); letter-spacing:-.25px; }
  .igh-hero__cta-row { display:flex; align-items:center; flex-wrap:wrap; gap:16px; padding-top:8px; }
  .igh-hero__btn { display:inline-flex; align-items:center; justify-content:center; gap:6px; padding:12px 20px; border:1px solid transparent; border-radius:12px; color:#fff; background:var(--heading-dark); font:650 15px/22px var(--font-body); letter-spacing:-.3px; text-decoration:none; transition:transform .3s,background .3s,border-color .3s; }
  .igh-hero__btn:hover { transform:translateY(-2px); background:#000; }
  .igh-hero__btn--outline { border-color:rgba(16,24,40,.22); color:var(--heading-dark); background:transparent; }
  .igh-hero__btn--outline:hover { border-color:var(--heading-dark); background:transparent; }
  .igh-hero__trust { display:flex; flex-wrap:wrap; gap:10px 18px; padding-top:4px; }
  .igh-hero__trust-item { position:relative; padding-left:16px; color:var(--heading-dark); font:400 15px/1.4 var(--font-body); }
  .igh-hero__trust-item::before { content:""; position:absolute; left:0; top:8px; width:6px; height:6px; border-radius:50%; background:#10b981; }
  .igh-hero__visual { position:relative; margin-right:calc(-1 * (max(0px,(100vw - 1128px) / 2) + 24px)); }
  .igh-hero__visual-card { position:relative; padding:14px; border:1px solid rgba(255,255,255,.62); border-right:0; border-radius:22px 0 0 22px; background:linear-gradient(180deg,#ffffff9e,#ffffff47),#ffffff57; backdrop-filter:blur(16px); }
  .igh-hero__visual-inner { position:relative; padding:20px; border-radius:16px 0 0 16px; overflow:hidden; background:linear-gradient(135deg,#d9ecfa,#eef6fc 45%,#fff); }
  .igh-hero__visual-inner img { display:block; width:100%; height:auto; border:1px solid rgba(29,41,57,.1); border-radius:12px; box-shadow:0 20px 50px rgba(30,52,92,.16); }
  .igh-trust-strip { padding:1.5rem; border-top:1px solid var(--surface-card-border); border-bottom:1px solid var(--surface-card-border); }
  .igh-trust-strip__inner { max-width:1280px; margin:0 auto; display:flex; align-items:center; justify-content:center; flex-wrap:wrap; gap:1rem 2.25rem; }
  .igh-trust-strip__stat { color:var(--subheading-gray); font:400 .875rem/1.4 var(--font-body); white-space:nowrap; }
  .igh-trust-strip__stat b { color:var(--heading-dark); font-weight:700; }
  @media (max-width:900px) { .igh-hero__grid { grid-template-columns:1fr; } .igh-hero__visual { margin-right:-24px; } .igh-hero__title { font-size:52px; line-height:1.05; } }
  @media (max-width:767px) { .igh-hero { padding:128px 18px 48px; } .igh-hero__content { align-items:center; text-align:center; } .igh-hero__title { font-size:42px; line-height:1.04; letter-spacing:-1.7px; } .igh-hero__desc { font-size:16px; line-height:24px; } .igh-hero__cta-row { width:100%; } .igh-hero__btn { width:100%; } .igh-hero__trust { justify-content:center; } .igh-hero__visual { width:calc(100% + 18px); margin-right:-18px; } .igh-hero__visual-inner { padding:14px; } .igh-trust-strip__inner { align-items:flex-start; flex-direction:column; gap:12px; } }
</style>
