---
// Dark stats section — engagement text + metrics (matching speaker-management pattern)
const stats = [
  { value: "0%", label: "Platform Fee Option" },
  { value: "50K+", label: "Tickets Processed" },
  { value: "99.9%", label: "Uptime Reliability" },
  { value: "4.8/5", label: "Organizer Satisfaction" },
];
---

<section class="etd-dark">
  <div class="etd-engagement">
    <div class="etd-engagement-text">
      <h2 class="etd-heading animate-on-scroll" data-animation="fadeInDown">
        <span class="etd-white">Sell tickets, manage payments, and </span>
        <span class="etd-purple">track every check-in </span>
        <span class="etd-white">from </span>
        <span class="etd-cyan">one branded workflow</span>
      </h2>
    </div>
  </div>

  <div class="etd-stats">
    {stats.map((stat) => (
      <div class="etd-stat">
        <span class="etd-stat-value">{stat.value}</span>
        <span class="etd-stat-label">{stat.label}</span>
      </div>
    ))}
  </div>
</section>

<style is:global>
  .etd-dark {
    display: flex;
    flex-direction: column;
    width: 100%;
    padding: 110px 0;
    background-color: #000;
    background-image:
      radial-gradient(at 57% 33%, hsl(219 0% 0%) 0px, transparent 50%),
      radial-gradient(at 46% 60%, hsl(219 100% 15%) 0px, transparent 50%),
      radial-gradient(at 60% 56%, hsl(327 100% 11%) 0px, transparent 50%),
      radial-gradient(at 49% 0%, hsl(307 100% 11%) 0px, transparent 50%),
      radial-gradient(at 96% 0%, hsl(195 97% 13%) 0px, transparent 50%),
      radial-gradient(at 96% 97%, hsl(206 100% 24%) 0px, transparent 50%),
      radial-gradient(at 8% 82%, hsl(293 96% 10%) 0px, transparent 50%),
      radial-gradient(at 0% 0%, hsl(196 100% 21%) 0px, transparent 50%),
      url('https://cms.eventhex.ai/wp-content/uploads/2024/12/bg-1.webp');
    background-position: center center;
    background-repeat: no-repeat;
    background-size: cover;
  }

  .etd-engagement {
    width: 100%;
    padding: 0 0 60px;
    display: flex;
    justify-content: center;
  }

  .etd-engagement-text {
    width: 65%;
  }

  .etd-heading {
    font-family: var(--font-heading);
    font-size: 48px;
    font-weight: 400;
    line-height: 52px;
    letter-spacing: -1px;
    color: #fff;
    text-align: center;
    margin: 0;
  }

  .etd-white { color: #fff; }
  .etd-purple { color: #AFA3FF; }
  .etd-cyan { color: #ABFFFF; }

  .etd-stats {
    display: flex;
    justify-content: center;
    gap: 60px;
    padding: 0 20px;
    max-width: 1100px;
    margin: 0 auto;
    width: 100%;
  }

  .etd-stat {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 6px;
  }

  .etd-stat-value {
    font-family: var(--font-heading);
    font-size: 40px;
    font-weight: 700;
    line-height: 48px;
    letter-spacing: -1px;
    color: #fff;
  }

  .etd-stat-label {
    font-family: var(--font-body);
    font-size: 15px;
    font-weight: 400;
    line-height: 20px;
    color: rgba(255, 255, 255, 0.6);
  }

  @media (max-width: 1024px) {
    .etd-dark { padding: 80px 0; }
    .etd-engagement { padding: 0 20px 40px; }
    .etd-engagement-text { width: 85%; }
    .etd-heading { font-size: 40px; line-height: 1.1em; }
    .etd-stats { gap: 40px; }
    .etd-stat-value { font-size: 32px; }
    .etd-stat-label { font-size: 14px; }
  }

  @media (max-width: 767px) {
    .etd-dark { padding: 64px 0; }
    .etd-engagement { padding: 0 15px 30px; }
    .etd-engagement-text { width: 100%; }
    .etd-heading { font-size: 25px; line-height: 1.2em; letter-spacing: 0; }
    .etd-stats {
      flex-wrap: wrap;
      gap: 24px;
      justify-content: center;
    }
    .etd-stat { width: 40%; }
    .etd-stat-value { font-size: 28px; }
    .etd-stat-label { font-size: 13px; }
  }
</style>
