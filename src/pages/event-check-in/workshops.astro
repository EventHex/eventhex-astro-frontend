---
import FeatureEventTypePage from "../../components/programmatic/FeatureEventTypePage.astro";
import { featureEventTypeData } from "../../lib/programmatic/feature-event-type-data";

const entry = featureEventTypeData.find(
  (e) => e.feature === "event-check-in" && e.eventType === "workshops"
)!;
---

<FeatureEventTypePage entry={entry} />
