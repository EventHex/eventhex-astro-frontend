interface:
  display_name: "Elementor to Astro Converter"
  short_description: "Convert Elementor widgets and pages to Astro components"
  default_prompt: "Use $elementor-to-astro to convert Elementor page content to Astro components, preserving structure, styling, responsive behavior, and animations."

policy:
  allow_implicit_invocation: true
