interface:
  display_name: "Elementor Widget Extractor"
  short_description: "Read-only raw + normalized Elementor extraction"
  default_prompt: "Use $elementor-widget-extraction to extract a named mode such as page-skeleton, subtree, page-assets, plugin-assets, or handoff-bundle from Elementor without mutating content."

policy:
  allow_implicit_invocation: false