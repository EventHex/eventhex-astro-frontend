interface:
  display_name: "Elementor Widget Extractor"
  short_description: "Extract complete widget info from Elementor pages"
  default_prompt: "Use $elementor-widget-extraction to extract widget data, text, styling, and configurations from Elementor pages via the novamira/execute-php MCP ability."

policy:
  allow_implicit_invocation: true
